# LibrePlay PMO Master Checklist

Status: `LAN-DEMO-READY / CREATOR-COMMERCE-HARDENED / PRODUCTION-NO-GO`
Last updated: 2026-07-10 CEST
Target: `https://libreplay.e-dani.com`

Current production overlay: `NOT-PRODUCTION-READY`.
Evidence: [libreplay-production-readiness-pmo.md](./libreplay-production-readiness-pmo.md) records the PMO audit and follow-up validation. LAN validation, release automation, media worker foundation, image/video media groundwork, provider-neutral media/payment scaffolds, security mutation and Authz/BOLA gates, Redis-backed abuse budgets, staging contracts/intake, mobile/PWA/i18n gates, process-level web/worker HA, runtime metrics/alerts/dashboard, LAN uptime synthetic checks, multi-window LAN synthetic SLO policy, read-only route rehearsal, log aggregation policy, structured runtime logging, backup credential contract, non-web auth/jobs console hardening and dormant media orphan inventory GitOps contract are now prepared. Production is still blocked by real provider secrets, real SMTP delivery in staging/prod, staging mailbox secrets, approved real PSP integration, live CDN/signed media activation, manual Vault bootstrap + human evidence + Security/PMO dual signoff for orphan inventory activation, physical media purge/orphan cleanup execution, real media moderation, real-device mobile matrix, public trusted-proxy runtime proof, node/stateful HA, live scoped MinIO/PG backup credential provisioning and activation, media backup/restore rehearsal, non-destructive restore rehearsal, DR, compliance, external error-tracking activation, a dedicated LibrePlay receiver/pager or owner-approved real-fire rehearsal, residual dependency latency tuning and root-cause cleanup.

Update 2026-06-23: `PROD-OBS-DEPS-FLAP-04` mitigated the dependency readiness flapping blocker. Redis `TCP_TIMEOUT`, readiness `503` flapping and multi-second Meilisearch samples did not recur in post-rollout monitoring; residual dependency latency tuning remains because Meilisearch/DNS samples still show intermittent `~170-189ms` spikes.

Update 2026-06-24: `PROD-OBS-ALERT-SLO-05` closes the safe GitOps slice for multi-window LAN synthetic SLO policy, read-only Alertmanager route rehearsal and Loki log aggregation policy. External error tracking, a dedicated LibrePlay receiver/pager or owner-approved real-fire rehearsal, and residual dependency latency/root-cause cleanup remain open.

Update 2026-06-24: `PROD-MEDIA-PURGE-ORPHAN-02` docs/cleanup: a GitOps contract for dormant media orphan inventory is now complete. Contract files (`ops/libreplay-media-orphan/*`), checkers (`scripts/check-libreplay-media-orphan-*.sh`), and activation template are ready. Contract is outside k8s/kustomization.yaml, CronJob remains suspend: true. Manual bootstrap of Vault secret/libreplay/media-orphan (scoped MinIO/PG credentials, no ClusterSecretStore automation), ExternalSecret Ready status, operator activation evidence, and Security AND PMO written signoff are all required before activation. Physical purge execution remains blocked pending separate legal/backup/MinIO-versioning approval.

Update 2026-07-03: PMO autopilot closed `LAN-DEMO-READY` for `https://libreplay.lan.e-dani.com`. Final source SHA `4e38d8e9b6dc` is CI-green (`28633453078`) and released (`28633695336`) with web digest `sha256:63e10191aeed814b8c9bc1b5ee6a760a2ec64ec2f774712269bbecf4dd9f226e`, worker digest `sha256:fdae4b7cd763f43642c45c0bd86d25c8b1f48288790f996d570acf6508a7c456`, tools digest `sha256:949a681e9dddf331cda3a7827f5d42c9a22b20d04b67d47578edf8bdb8fbd8d3`, and seed digest `sha256:1dd8a937d0df54572cbc7f5a69c856a9ee86a1f58b4e3e16b48fb0820dd4162b`. GitOps commit `8b40006` is Argo `Synced/Healthy`; runtime config reports `LIBREPLAY_VERSION=4e38d8e9b6dc`, `USE_MOCK_OAUTH=true`, `ENABLE_LAN_DEMO_LOGIN=true`; `/api/health`, `/api/health/deps` and `/api/auth/demo-users` return HTTP `200`; web endpoints are populated; Playwright LAN full returned `103 passed (2.1m)` with trace/screenshot/video enabled and zero critical skips. Production monetization remains `NO-GO`; LAN demo mocks remain allowed only as `lan-demo/no-prod`.

Update 2026-07-07: PMO corrected the creator showcase gap reported from `https://libreplay.lan.e-dani.com/es/creators/cmqk3dlke000s6gysm850m6lz`. The page now renders `Galeria`, `Directos`, `Tiers`, `Contenido premium`, `Contenido de pago` and `Subasta LAN`; `Demo Creator` seed data now has `gallery=4`, `ppv=2`, `paid=1`, `live LIVE/PAID_PRIVATE=1`, `live SCHEDULED/SUBSCRIBERS_ONLY=1` and `postmedia=3`; four media API checks returned `HTTP 200 image/svg+xml`; focused Playwright LAN returned `1 passed` with trace/screenshot/video. Runtime is deployed from GitOps commit `7ff4557` with web image `harbor.e-dani.com/homelab/libreplay-web:sha-5904bd1da935@sha256:58094f0e5b0e6b52e46f7ba745b5ac76a71dafc7a0a20764b1882ec59c2d9653`. Known gaps remain: auctions are a LAN demo widget without persistent bid backend, and live streaming is LAN/no-prod playback/ticket validation rather than a production streaming stack.

Update 2026-07-10: PMO closed the hardened creator-commerce LAN gate. Source PR `pocharlies-org/libreplay#1` merged as `85b599a3cc5253f9c191eb59247d4c5863e9e3cd`; CI `29097347671` and release `29098048260` passed. GitOps PR #6 merged as `1ade75ceacce1a740298178484256425610dac3a`; CI `29099449442` passed and Argo is `Synced/Healthy`. Strict migration and seed Jobs completed with `100/100 skipped=0`, and a second seed execution proved idempotent. Full Playwright LAN passed `108/108` in 2.6m with zero retries and zero critical skips. Post-E2E ledger reconciliation found `13` balanced transactions, `0` imbalances, `0` wallet/reservation mismatches, `0` negative available wallets and `0` duplicate tip keys. Namespace PolicyReports finish at pass `188`, fail/warn/error `0`, skip `4`. Production remains `NO-GO` pending real LiveKit edge/secrets/e2e, adult PSP, KYC/age/face/CSAM/moderation and legal/tax/payout/compliance evidence.

## 2026-07-10 PMO Closure - Hardened Creator Commerce LAN

- [x] Source passed independent RHO verification and PR/CI gates. Evidence: final verifier verdict at source `a2045a7` was `ACCEPT` with no P0/P1/P2; PR `https://github.com/pocharlies-org/libreplay/pull/1` merged as `85b599a3cc5253f9c191eb59247d4c5863e9e3cd`; CI run `29097347671` passed.
- [x] Immutable release is published and inspected. Evidence: web digest `sha256:4d702a9c01fd8e85529b57a0122573a704b6cc3807935ccb3a1dadce4587098b`; tools `sha256:456e038cf0503e71af1be494bc4d1a05d16f1d12c9d2fe5c59b728e7629cb81b`; seed `sha256:dcab2bfd7b4754985e82eec5d8b8adc2f9ad0d1bfe8c4c0f43d7403db91721b4`; worker `sha256:3dc0ccc759f358fc0e4435c352384aced1a5b9a295d40cbac7b807b29a5a14ed`.
- [x] GitOps promotion passed PR, CI and Argo gates. Evidence: PR `https://github.com/pocharlies-org/k8s-libreplay-pocharlies/pull/6` merged as `1ade75ceacce1a740298178484256425610dac3a`; CI `29099449442` passed no-latest, YAML, kubeconform and all overlays; Argo reports `Synced/Healthy`.
- [x] Migration and strict seed completed without destructive data operations. Evidence: `libreplay-db-migrate-85b599a` completed in 10s; `libreplay-db-seed-85b599a` completed in 5m47s with `100/100 skipped=0`; a second cloned seed Job also completed `100/100 skipped=0` with unchanged seed financial counts and balanced journals.
- [x] Creator commerce works through real LAN mutations. Evidence: Playwright covers persisted creator tip debit, persisted auction reservation, creator authoring forms/live controls, upload/gallery, subscription and PPV payments, creator showcase and role-specific admin/creator/member flows.
- [x] Full cross-role LAN browser gate is green. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWVIDEO=on PWSCREENSHOT=on pnpm --filter @libreplay/web exec playwright test --trace on` returned `108 passed (2.6m)` across anon/member/admin/creator/clubowner/newbie/mobile; report and 108 traces are archived under the source workspace Playwright artifacts.
- [x] Post-E2E runtime and finance checks are green. Evidence: `/api/health` and `/api/health/deps` return 200; queues have no waiting/active/delayed/failed work; web/worker log scans found no critical errors or credential/object-key leaks; ledger transactions `13`, imbalanced `0`, wallet/reservation mismatches `0`, negative available wallets `0`, duplicate tip keys `0`.
- [x] LibrePlay namespace policy gate is clean. Evidence: two completed manual image-probe pods were removed; final PolicyReport aggregate is pass `188`, fail `0`, warn `0`, error `0`, skip `4`.
- [blocked] Production live streaming remains incomplete. Required: deployed LiveKit namespace/app, real API secrets, DNS/WSS, RTC/TURN edge, egress/CDN, recording policy and real multi-browser creator/viewer/admin e2e.
- [blocked] Production monetization and compliance remain incomplete. Required: adult-approved PSP and commercial agreement, production KYC/age/face-liveness/CSAM/media-moderation providers, staging/production ExternalSecrets, legal/tax/payout/refund/dispute approval and compliance signoff.

## 2026-07-07 PMO Correction - Creator Showcase

- [x] Creator page no longer shows an empty shell. Evidence: `https://libreplay.lan.e-dani.com/es/creators/cmqk3dlke000s6gysm850m6lz` renders `Galeria`, `Directos`, `Tiers`, `Contenido premium`, `Contenido de pago` and `Subasta LAN`.
- [x] DB seed corrected without deleting old data. Evidence: `Demo Creator` now has `gallery=4`, `ppv=2`, `paid=1`, `live LIVE/PAID_PRIVATE=1`, `live SCHEDULED/SUBSCRIBERS_ONLY=1`, and `postmedia=3` in `libreplay_lan`.
- [x] Pending LAN DB migrations applied before reseed. Evidence: migrations through `20260704007000_finance_export_preview_audit_action` were applied by `pnpm --filter @libreplay/db migrate:deploy`.
- [x] Creator media is real and browser-reachable through the first-party media API. Evidence: four `/api/media/{id}?variant=THUMB_MEDIUM` checks returned `HTTP 200` with `image/svg+xml` bytes.
- [x] Runtime deployed from GitOps. Evidence: GitOps commit `7ff4557 libreplay: deploy creator showcase web image`; Argo revision `7ff4557c8d7d2826c87ecf4906beec6e00bf8441`, sync `Synced`, health `Healthy`; web pods run `harbor.e-dani.com/homelab/libreplay-web:sha-5904bd1da935@sha256:58094f0e5b0e6b52e46f7ba745b5ac76a71dafc7a0a20764b1882ec59c2d9653`.
- [x] Focused Playwright LAN creator validation passed. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWVIDEO=on PWSCREENSHOT=on pnpm --filter @libreplay/web exec playwright test e2e/45-creator-showcase.auth.spec.ts --project=member --trace on` returned `1 passed`.
- [x] Visual evidence archived locally. Evidence: full-page screenshot `apps/web/test-results/creator-showcase-fullpage.png`; Playwright artifacts under `apps/web/test-results/45-creator-showcase.auth-C-d9198--packs-and-auction-controls-member/`.
- [known gap] Auction is a LAN demo widget only; bids are not persisted to a real auction/bid backend model yet.
- [known gap] Creator live streaming is LAN/no-prod playback/ticket flow for validation; it is not a production-grade streaming stack.

## 2026-07-03 PMO Autopilot - LAN Operability And Monetization Hardening

- [x] LAN baseline checked before changes. Evidence: Argo `libreplay` is `Synced/Healthy`; namespace `libreplay` has Postgres, MinIO, Meili, web and worker pods ready; `/api/health` and `/api/health/deps` return HTTP `200`.
- [x] Local source gate for new monetization foundation is green. Evidence: `pnpm -r typecheck`, `pnpm -r test`, `pnpm -r lint`, `pnpm --filter @libreplay/web build`, Prisma validate and `git diff --check` passed in `/home/dibanez/k8s/libreplay`; lint has existing non-blocking Next/React warnings only.
- [x] Docker release targets no longer miss the new workspace package. Evidence: Dockerfile copies `packages/monetization/package.json`; `docker build --target runner`, `--target tools`, `--target seed` and `--target worker` pass with frozen workspace install/build.
- [x] Playwright LAN findings from the first browser pass were fixed in source. Evidence: Spanish forgot-password copy now says `¿Has olvidado tu contraseña?`; dark muted foreground contrast was raised to avoid the serious axe contrast violation seen on `/es/discover` mobile.
- [x] Payment-event recovery is audit-backed. Evidence: admin requeue route tests assert `AuditLog(action=PAYMENT_EVENT_REQUEUED)` and worker replay tests pass.
- [x] Subscription provider lifecycle is monotonic. Evidence: Stripe provider event timestamps are stored and older events are ignored; regression test covers a stale active delivery after a newer cancellation.
- [x] Refund execution is two-admin and UI-backed for LAN/P0. Evidence: source `01deea1 feat(monetization): add admin refund request UI` adds `/admin` refund request create/approve controls, i18n parity, idempotency/open-request tests and roadmap evidence.
- [x] Source release is published to Harbor with immutable tags. Evidence: GitHub Release Image run `28631260391` completed success after a transient Harbor LAN DNS rerun; source SHA `01deea107370`; web digest `sha256:85a9925fbbd942268809d05361e101c3612794d96658a3d058b7fecf5e400ba9`; tools digest `sha256:9b18ed132b2db84009b780bbcf793e7c3e30577d65d3960cddbc027d1590752a`; seed digest `sha256:40fbba21e65caa953e22aa5247d596fc0d8d50329ec19bf76cae060292f36ff3`; worker digest `sha256:59c82ba067d103fc2123f8d9956ffbd0537c7250974e39cc44bedd1b2d947474`.
- [x] GitOps rollout candidate is dry-run clean. Evidence: `kubectl apply --dry-run=server -f k8s/manifest.yaml`, `kubectl apply --dry-run=server -k k8s`, `kubectl kustomize k8s`, `git diff --check` and `rg ':latest|libreplay-web:latest' k8s/manifest.yaml` passed; new jobs `libreplay-db-migrate-01deea1` and `libreplay-db-seed-01deea1` are created server-side in dry-run.
- [x] Final LAN acceptance fixes are deployed to LAN. Evidence: source commit `4e38d8e fix(e2e): close LAN demo acceptance gaps`; Release Image run `28633695336`; GitOps commit `8b40006 deploy(libreplay): roll out LAN acceptance fixes`; Argo `libreplay` is `Synced/Healthy` at `8b4000628766c783275125540ee2aba943598e74`; web and worker rollouts completed on the expected digest-pinned images.
- [x] Full LAN acceptance is closed. Evidence: `/api/health`, `/api/health/deps` and `/api/auth/demo-users` return HTTP `200`; health deps show postgres, redis, queues, MinIO and Meili `ok:true`; queues have `failed=0`; Playwright LAN full command `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWVIDEO=on PWSCREENSHOT=on PWJSON=apps/web/test-results/lan-full-4e38d8e.json pnpm --filter @libreplay/web exec playwright test --trace on` returned `103 passed (2.1m)`; policy reports have no fail/warn/error in namespace `libreplay` (only four `skip` label policy results); post-rollout web/worker log scan over the last 3 minutes found no critical errors.
- [blocked] Production monetization remains blocked. Required before public/prod: written adult PSP approval, legal/tax/Perlas terms, real age/KYC/KYB/face/CSAM/media moderation providers, payout provider execution, refund/dispute partial policy, reserve release policy, audit retention/export and compliance signoff.
- [blocked] Partial refunds and partial disputes remain fail-closed by amount mismatch in P0; full-transaction reversal is supported and tested.

## 2026-06-24 PMO Iteration - Media Orphan Inventory Runtime Evidence

- [x] Orphan inventory source exists in current runtime lineage. Evidence: source commit `7da4345 Add safe media orphan inventory scaffold` is an ancestor of current `main` and deployed runtime `48a9dd094926`.
- [x] Live worker can execute a bounded read-only inventory without raw object-key output. Evidence: PMO called `buildMediaOrphanInventory({ maxPages: 20, pageSize: 1000, minObjectAgeHours: 24 })` directly in the `libreplay-worker` pod, avoiding AuditLog writes; output was sanitized counts only: `scanned.objects=1094`, `scanned.pages=2`, `scanned.truncated=false`, `referenced.assetKeys=206`, `referenced.variantKeys=852`, `orphans.total=36`, `orphans.byTopPrefix.variants=36`, `orphans.protectedPrefixHits=106`.
- [x] Local focused tests cover orphan inventory and fail-closed physical purge. Evidence: `pnpm --filter @libreplay/jobs test -- src/handlers/media-orphan-inventory.test.ts src/handlers/media-physical-purge-contract.test.ts` returned `22/22`; `pnpm --filter @libreplay/jobs test -- src/handlers/media-retention.test.ts` returned `7/7`.
- [x] Jobs package does not reference S3 delete primitives. Evidence: `rg -n "deleteObject|DeleteObjectCommand|DeleteObjectsCommand" packages/jobs/src packages/jobs/package.json` returned no matches.
- [blocked] Physical deletion of current orphan candidates remains blocked. Evidence: `packages/jobs/src/handlers/media-physical-purge-contract.ts` throws `approval_gate_unset` unconditionally; legal/backup/MinIO-versioning approval and Security/PMO signoff are still required before any delete worker exists.
- [x] Dormant media orphan inventory GitOps contract is prepared (PROD-MEDIA-PURGE-ORPHAN-02 complete). Evidence: `ops/libreplay-media-orphan/` contains ExternalSecret, CronJob (suspend: true), policies, SQL, README and activation-evidence template; `scripts/check-libreplay-media-orphan-contract.sh` and `scripts/check-libreplay-media-orphan-activation.sh` pass; contract outside k8s/kustomization.yaml; character normalization and PMO doc updates complete.
- [blocked] Orphan inventory activation requires manual Vault bootstrap + human evidence. Evidence: `secret/libreplay/media-orphan` must be populated manually (no ClusterSecretStore bootstrap in cluster scope); operator must fill activation-evidence template with redacted proof (MinIO policy, PG grants, Vault path confirmed, no admin/root creds, no physical-purge primitive); Security AND PMO must sign off before ExternalSecret is added to Argo-synced path and CronJob is un-suspended.
- [blocked] Physical media purge execution remains gated. Evidence: `packages/jobs/src/handlers/media-physical-purge-contract.ts` throws `approval_gate_unset` unconditionally; no --execute path in CLI; separate legal/backup/MinIO-versioning approval and Security/PMO signoff required before any delete worker or CronJob unblock.

## 2026-06-24 PMO Iteration - Multi-window SLO, Route Rehearsal And Log Policy

- [x] Research, architecture and security gates completed before implementation. Evidence: RHO reports under `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-obs-alert-slo-05/`; Researcher identified the `severity=warning` blackhole and live Loki sink, Architect defined the 99.0%/14d LAN synthetic SLO contract, and Security approved with read-only routing and no-secret conditions.
- [x] Canonical multi-window LAN synthetic SLO is declared. Evidence: `k8s/vmrule.yaml` appends group `libreplay.slo` with 6 recording rules (`ratio5m`, `ratio30m`, `ratio1h`, `ratio6h`, `ratio3d`, `error_budget_remaining_ratio`) and 3 burn-rate alerts (`LibrePlaySLOErrorBudgetBurnFast`, `Medium`, `Slow`) using `critical` for fast/medium and `warn` for slow.
- [x] Existing SLO proxy no longer blackholes. Evidence: `LibrePlaySyntheticAvailabilityBudgetLow` now uses `severity=warn`; `rg -n 'severity:\s*warning' k8s/vmrule.yaml` returned no matches.
- [x] Staging cannot inherit LAN-derived SLO rules. Evidence: `staging/overlays/runtime/patch-remove-lan-synthetics-vmrule.json` removes `/spec/groups/2` before `/spec/groups/1`; `scripts/check-libreplay-synthetics-contract.sh` confirms base includes `libreplay.slo` and staging excludes it.
- [x] Read-only route rehearsal passes against live shared Alertmanager. Evidence: `scripts/check-libreplay-alert-routing.sh` runs `amtool config routes test` only; live rehearsal resolved `LibrePlaySyntheticAvailabilityBudgetLow`, `LibrePlaySLOErrorBudgetBurnFast`, `LibrePlaySLOErrorBudgetBurnMedium` and `LibrePlaySLOErrorBudgetBurnSlow` to `monitoring-synapse-webhook-synapse-alertmanager`, with no blackhole receiver.
- [x] Log aggregation policy is documented without copying secrets. Evidence: `docs/libreplay-log-aggregation-policy.md` records Alloy -> Loki, `namespace="libreplay"` / `namespace="libreplay-staging"` selectors, 336h/14d retention, label-index-only health checks and the out-of-scope plaintext MinIO credential residual without values.
- [x] Alert routing runbook is documented. Evidence: `docs/libreplay-alert-routing-runbook.md` records the shared Synapse receiver posture, no dedicated LibrePlay pager, no active external error tracking, and owner-approved/time-boxed/manual real-fire guardrails.
- [x] PMO validation gates passed. Evidence: `sh -n` passed for both checkers; `scripts/check-libreplay-synthetics-contract.sh` passed; `scripts/check-libreplay-alert-routing.sh` passed; base and staging kustomize renders passed; client and server dry-runs passed; `git diff --check` passed; sensitive-string gates found no forbidden Alertmanager config-dump, alert-injection, Loki log-content or credential-literal patterns in the new checker/docs.
- [blocked] Production observability remains incomplete outside this safe slice. Evidence: external error tracking remains unconfigured; alerts still route to the shared Synapse receiver, not a dedicated LibrePlay pager; owner-approved real-fire rehearsal is documented but not executed; residual dependency latency/root-cause cleanup remains open.
- [blocked] PMO process exception is recorded. Evidence: Claude CLI DevOps produced the main YAML/checker edits but repeatedly hung before docs/report; PMO completed the narrow docs/microfix recovery and recorded `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-obs-alert-slo-05/pmo-recovery-audit.report.md`.

## 2026-06-23 PMO Iteration - Dependency Readiness Flapping Mitigation

- [x] Root cause was narrowed without masking dependency failures. Evidence: source investigation found Redis FQDN connection/DNS overhead and sequential dependency checks made `/api/health/deps` vulnerable to readiness timeouts; route semantics still return `503` when any dependency check fails.
- [x] Source fix is committed, CI-green and released. Evidence: source commit `908bb7a fix(observability): stabilize dependency health checks` parallelized dependency checks, resolved Redis host before TCP and bounded Meili fetch timeout; source commit `48a9dd0 fix(observability): cache dependency tcp resolution` added bounded TCP target caching; source CI `28049234713` completed `success`; Release Image `28049733196` completed `success` for `sha-48a9dd094926`.
- [x] GitOps rollout is deployed and healthy. Evidence: GitOps commit `034dfa2 deploy(libreplay): rollout cached dependency health checks`; GitOps CI `28050549553` completed `success`; Argo `libreplay` is `Synced|Healthy|034dfa2c8b941e2fb711b9f5ee0b6e6fa2f2aa69|Succeeded`; web and worker are `2/2` on digests `sha256:5f8bfe5bfa840fa0527dbc3289c0ed22302387cf575aa5845006bc99c35268a0` and `sha256:721def1105cef0345fe37663ee4c2c0d74b9fa6a1c8ceeaaaaf4a95f236a6d3f` with zero restarts.
- [x] Post-rollout dependency health no longer flaps. Evidence: `/api/health` returned HTTP `200`; two 30-sample `/api/health/deps` windows from `2026-06-23T21:14:37+02:00` through `2026-06-23T21:16:27+02:00` returned `60/60` HTTP `200` and `ok:true`, no failed checks, no Redis `TCP_TIMEOUT`, Redis max `169ms`, Meili max `183ms`; a later 2-minute monitor from `2026-06-23T21:18:02+02:00` returned `24/24` HTTP `200`, no failed checks, no new warning events, Redis max `174ms`, Meili max `189ms`.
- [x] Logs and resource checks stayed clean. Evidence: web logs from the rollout window had no `error|warn|tcp_timeout|timeout|p1001|readablestream|secret|token|password` matches; web/worker pods are ready with zero restarts; node/pod metrics showed no saturation.
- [blocked] Residual dependency latency tuning remains. Evidence: Meilisearch and occasional Redis samples still show `~170-189ms` latency spikes even after the readiness flapping fix; this is no longer producing `503`, but should be handled in a follow-up SRE cleanup.

## 2026-06-23 PMO Iteration - Non-Web Structured Log Migration

- [x] Research, architecture, security and independent verification passed. Evidence: RHO reports under `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-obs-nonweb-logs-03/`; Researcher inventoried residual non-web `console.*` sites across auth and jobs packages, Architect approved structured/redacted logger extension for auth console emails and jobs CLI stderr, Security approved with safe-field conditions, and Verifier returned `PASS`.
- [x] Source migration is committed and CI-green. Evidence: source commit `857de79 fix(observability): harden non-web runtime logging`; source SHA `857de79078aa1b7c08cd539b548742005662c720`; source CI run `27919082739` completed `success`; auth console email logs no longer include recipient email, action URL or token; jobs CLI stderr uses the structured/redacted logger; jobs stdout JSON is explicitly marked `libreplay: cli-stdout-contract`; `no-console` guards added for auth/jobs production-adjacent source.
- [x] Local and independent test evidence is green. Evidence: verifier report `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-obs-nonweb-logs-03/verifier.report.md` returned `PASS`; auth tests `38/38`, jobs tests `50/50`, observability tests `14/14`, full workspace tests and typecheck passed; console scan found only 2 approved CLI stdout contract lines.
- [x] Media-retention tests assert no raw object keys in serialized output. Evidence: verifier confirmed media-retention tests assert serialized result contains no raw object keys.
- [x] Release image is published. Evidence: Release Image run `28044613466` completed `success`; web image `harbor.e-dani.com/homelab/libreplay-web:sha-857de79078aa@sha256:ef37a82e70074f6362b8067a2fae93882216c62b5a685e452e6ab8c7b3ca7bdf`; worker image `harbor.e-dani.com/homelab/libreplay-web:worker-sha-857de79078aa@sha256:77d6be492673087985c11d02c1355a581024082dfa40ec0ee31e24548bcd2141`.
- [x] GitOps rollout is deployed and healthy. Evidence: GitOps commit `aa36e61 deploy(libreplay): rollout non-web structured logs`; GitOps CI run `28045245793` completed `success`; Argo `libreplay` is `Synced|Healthy|aa36e610f9f1a3fd73f3ef7b546c1aa5cd3974e4|Succeeded`; web and worker are `2/2` with zero restarts on the expected `857de79078aa` digests; runtime config is `LIBREPLAY_VERSION=857de79078aa`, `LOG_LEVEL=info`, `ERROR_TRACKING_PROVIDER=none`, `DEPLOYMENT_MODE=lan-demo`.
- [x] Runtime smoke and log scan passed. Evidence: `/api/health` returned HTTP `200` and echoed `x-request-id: req-nonweb-logs-03`; `/api/health/deps` sample returned postgres, redis, queues, minio and meilisearch `ok:true` with authEmail/mediaModeration queues all `0`; strict web and worker log scans found no `level=error`, exception, secret, token, password, `redis://`, `postgres://`, actionUrl, resetUrl, Bearer or cookie matches; worker logs are JSON structured with `version="857de79078aa"`.
- [blocked] Production observability remains incomplete outside this meta. Evidence: this gate closes non-web auth/jobs console hardening and `no-console` guards for auth and jobs production-adjacent source, but external error tracking, alert fire/route rehearsal, multi-window SLO/error-budget policy, log aggregation sink policy and residual dependency latency tuning remain open.
- [x] Dependency readiness flapping was investigated in the next PMO meta. Evidence: `PROD-OBS-DEPS-FLAP-04` source commits `908bb7a` and `48a9dd0`, release `28049733196`, GitOps commit `034dfa2`, and post-rollout samples closed the Redis `TCP_TIMEOUT`/multi-second Meili readiness blocker; residual `~170-189ms` latency tuning remains tracked separately.

## 2026-06-21 PMO Iteration - Web Runtime Structured Log Migration

- [x] Research, architecture, security and independent verification passed. Evidence: RHO reports under `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-obs-web-logs-02/`; Researcher inventoried the residual web runtime `console.error` sites, Architect approved `webLogger`/`getRouteLogger` plus guard design, Security approved with safe-field conditions, and Verifier returned `PASS`.
- [x] Source migration is committed and CI-green. Evidence: source commit `7b6b3a7 fix(observability): migrate web runtime logs`; source CI run `27917807302` completed `success`; all 13 high-risk web runtime log sites in rate-limit, auth email, OAuth audit, media/admin stream and map tile proxy paths now use the shared redacted structured logger; runtime `console.*` guard and ESLint `no-console` are source-controlled.
- [x] Local and independent test evidence is green. Evidence: verifier report records focused web logging/auth/rate tests `22/22`, media/admin tests `19/19`, web typecheck, web lint with no `no-console` errors and observability tests `14/14`; PMO also ran full web tests `189/189`, web build and `git diff --check`.
- [x] Release image is published. Evidence: Release Image run `27917971011` completed `success` for `sha-7b6b3a7e8703`; web `sha-7b6b3a7e8703@sha256:51d4d2e891e32260080c46ad75df9545587e602641cdb7c929d77118a6dd39c4`; worker `worker-sha-7b6b3a7e8703@sha256:11f23936bcddceddd5f445a467e10214e71ea44b58968e09627a4bab9dbffeb2`.
- [x] GitOps rollout is deployed and healthy. Evidence: GitOps commit `b521ded deploy(libreplay): rollout web structured runtime logs`; GitOps CI run `27918202957` completed `success`; Argo `libreplay` is `Synced|Healthy|b521ded4ed718b3e51f6c50efee24f7b8e675a5d|Succeeded`; web and worker are `2/2` with zero restarts on the expected image digests and `LIBREPLAY_VERSION=7b6b3a7e8703`.
- [x] Runtime smoke and log scan passed. Evidence: `/api/health/deps` returned HTTP `200` with postgres, redis, queues, minio and meilisearch `ok:true` and queue counts all `0`; `/api/health` returned HTTP `200` and echoed `x-request-id: req-runtime-structured-logs-02`; recent web log scan found no `error|exception|failed|secret|token|password|redis://|postgres://` matches; worker logs show JSON `media reconcile loop started`, `worker started` and `media moderation reconcile completed` with version `7b6b3a7e8703` and `failed:0`.
- [blocked] Production observability remains incomplete outside this meta. Evidence: this gate closes high-risk web runtime logging and the direct `console.*` guard for `apps/web/src/app/api` and `apps/web/src/lib`, but external error tracking, alert fire/route rehearsal, multi-window SLO/error-budget policy, log aggregation sink policy and residual non-web CLI/job logging cleanup remain open.
- [blocked] PMO process exception is recorded. Evidence: two Claude CLI implementation attempts stalled without final report; PMO completed the narrow remaining logging edits under `pmo-exception-implementation.report.md`, and the independent verifier returned `PASS`.

## 2026-06-21 PMO Iteration - Structured Logging Scaffold and I18N H1 Guard

- [x] Source observability scaffold and runtime wiring are committed and validated. Evidence: source commits `68ae5a8 feat(observability): add structured logging scaffold` and `0aaba21 feat(observability): wire structured runtime logs`; source CI runs `27915606845` and `27916173176` completed `success`; Release Image `27916339962` completed `success` for `sha-0aaba2176f14`; local gates passed `@libreplay/observability` tests `14/14`, config env tests `33/33`, web middleware/prisma tests `9/9`, jobs worker tests `8/8`, full `pnpm -r typecheck`, full `pnpm -r test`, web build and `git diff --check`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `21b1f12 feat(libreplay): deploy structured observability runtime`; GitOps CI `27916642297` completed `success`; Argo `libreplay` is `Synced|Healthy|21b1f12cf55653ab9f1627109282c64d82f16e7c|Succeeded`; web image is `sha-0aaba2176f14@sha256:27a51d2abac013f758cba6a5d06f601835353741a98884ce8cca9a2fb164367a`; worker image is `worker-sha-0aaba2176f14@sha256:a01b4b8522ccdb4deb85cc8639491f019e976d6b02baf5ac3c9bdafedcf3dfe9`; runtime config is `LOG_LEVEL=info`, `ERROR_TRACKING_PROVIDER=none`, `LIBREPLAY_VERSION=0aaba2176f14`.
- [x] Runtime smoke passed after deploy. Evidence: web/worker rollouts completed; `/api/health/deps` returned `ok:true`; `curl -I` to `/api/health` with `x-request-id: req-observability1` returned HTTP `200` and echoed `x-request-id`; worker logs emitted JSON structured entries for `media reconcile loop started`, `worker started` and `media moderation reconcile completed` with version `0aaba2176f14`; recent web/worker secret-pattern scans for Bearer/auth/cookie/token/actionUrl/resetUrl/DB/Redis/password/secret returned no matches.
- [x] Five-locale i18n was re-smoked on the current runtime. Evidence: `pnpm --filter @libreplay/web test -- src/i18n/catalog-parity.test.ts` returned `11/11`; live `/es`, `/en`, `/fr`, `/de`, `/pt` returned HTTP `200` with matching `<html lang>`; focused LAN Playwright returned `e2e/01-landing.anon.spec.ts --project=anon` `5 passed` after test repair and `e2e/34-demo-visuals.auth.spec.ts --project=member` `3 passed`.
- [x] I18N landing H1 regression guard is source-controlled. Evidence: source commit `9ac6f56 test(i18n): assert locale-specific landing hero` updates only `apps/web/e2e/01-landing.anon.spec.ts`; local parity and landing E2E passed; source CI run `27916798426` completed `success`.
- [blocked] Full production observability remains incomplete. Evidence: this gate adds redacted structured logging scaffold, worker runtime JSON logs, request-id propagation and fail-closed/no-op error-tracking config, but broader web route logging still has raw `console.*` residuals, external error tracking is not activated, and alert fire/route rehearsal plus multi-window SLO/error-budget policy remain open.

## 2026-06-21 PMO Iteration - LAN Uptime Synthetics and SLO Proxy

- [x] Research/Architecture/Security approved a minimal LAN synthetic scope. Evidence: RHO reports `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-ops-slo-synthetics-01/researcher.report.md`, `architect.report.md` and `security.report.md`; decisions were `IMPLEMENTABLE_WITH_CONDITIONS`, `APPROVE_WITH_CONDITIONS` and `APPROVE_WITH_CONDITIONS`.
- [x] LAN shallow health synthetic is declared. Evidence: `k8s/vmprobe.yaml` adds `VMProbe/libreplay-lan-synthetic` in `monitoring`, uses existing `blackbox-exporter.monitoring.svc.cluster.local:9115`, module `http_alive_tls`, target `https://libreplay.lan.e-dani.com/api/health`, no auth and no TLS-skip setting.
- [x] SLO proxy alerts are declared. Evidence: `k8s/vmrule.yaml` adds group `libreplay.synthetics` with `LibrePlaySyntheticDown`, `LibrePlaySyntheticLatencyHigh` and `LibrePlaySyntheticAvailabilityBudgetLow`.
- [x] Staging runtime cannot inherit the LAN synthetic. Evidence: `staging/overlays/runtime/patch-delete-lan-vmprobe.yaml` and `patch-remove-lan-synthetics-vmrule.json` delete the base LAN probe and synthetic alert group from staging runtime render.
- [x] Static synthetics contract passes. Evidence: `scripts/check-libreplay-synthetics-contract.sh` validates VMProbe target/module/no-auth/no-`/deps`, kustomization wiring, staging exclusion and VMRule alerts.
- [x] GitOps deployment and runtime validation passed. Evidence: commit `7614cd6 feat(libreplay): add LAN uptime synthetics` is pushed; CI run `27914911079` completed `success`; Argo `libreplay` reports `Synced|Healthy|7614cd6d259ae4e58fb6dd44c3910e8801c1d8a3|Succeeded`; live `VMProbe/libreplay-lan-synthetic` and `VMRule/libreplay-runtime` are `operational`; blackbox returns `probe_success 1` and HTTP `200`; VictoriaMetrics has `probe_success{job="libreplay-lan-synthetic"}=1`; VMAlert loads the three `LibrePlaySynthetic*` rules as `inactive` with `health=ok` and no active synthetic alerts.
- [blocked] Full observability remains incomplete. Evidence: this gate closes uptime synthetic and a short-window SLO proxy only; the later structured logging scaffold closes the first worker/request-id slice, but full web log migration, external error tracking, alert fire/route rehearsal, full multi-window error-budget policy and recurrence/root-cause cleanup remain open.

## 2026-06-21 PMO Iteration - Backup Credential Contract Phase A

- [x] Research/Architecture/Security approved a dormant scoped backup credential contract. Evidence: RHO reports `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-dr-backup-02/researcher.report.md`, `architect.report.md` and `security.report.md`; Researcher returned `IMPLEMENTABLE_AFTER_SECURITY_APPROVAL`; Architect and Security returned `APPROVE_WITH_CONDITIONS`.
- [x] Dormant backup credential artifacts exist outside the Argo-synced path. Evidence: `ops/libreplay-backup/externalsecret.yaml`, `minio-backup-policy.json`, `postgres-backup-role.sql` and `README.md` exist, while `k8s/kustomization.yaml` does not reference `ops/libreplay-backup` or `libreplay-backup-secrets`.
- [x] Static backup contract validator passes. Evidence: `scripts/check-libreplay-backup-contract.sh` passed, validating explicit `secret/libreplay/backup` remoteRefs, no `dataFrom`, MinIO policy without delete/admin/wildcard permissions, read-only variable-driven PG role SQL and no obvious credential literals.
- [x] Current active render remains unchanged by the dormant contract. Evidence: `kubectl kustomize k8s` rendered `2042` lines; server dry-run passed with only pre-existing `last-applied-configuration` warnings; `git diff --check`, `scripts/check-libreplay-production-contract.sh` and `scripts/check-libreplay-staging-contract.sh --static` passed.
- [x] Phase B bootstrap path is security-scoped to manual/operator only. Evidence: `phase-b-researcher.report.md` returned `MANUAL_BOOTSTRAP_RECOMMENDED` and `phase-b-security.report.md` returned `APPROVE_MANUAL_ONLY`; in-cluster automation would require mounting MinIO root/admin or PG admin credentials and is therefore blocked.
- [blocked] Backup credential activation remains blocked. Evidence: `secret/libreplay/backup` is not proven populated, `ExternalSecret/libreplay-backup-secrets` is not active, MinIO scoped service account is not minted/proven, and PG `libreplay_backup` is not created/proven.
- [blocked] Backup/DR remains incomplete. Evidence: no logical PG/MinIO backup CronJobs are active and no non-destructive restore rehearsal has passed.

## 2026-06-21 PMO Iteration - Media Moderation Reconcile Redis-Ephemeral Precondition

- [x] Backend stale media-moderation reconcile source is committed and CI-green. Evidence: source commit `140431d feat(jobs): reconcile stale media moderation jobs` adds `reconcileStuckMediaModeration`; source CI `27912742994` completed `success`; RHO checklist `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-dr-backup-01/RHO-CHECKLIST.md` records the jobs suite `47/47`, `tsc --noEmit`, ASCII scan and `git diff --check` passing, and Backend decision `REDIS_EPHEMERAL_ACCEPTABLE_WITH_CONDITIONS`.
- [x] Release image was published. Evidence: Release Image `27912915607` completed `success` for `sha-140431d86db3`; web `harbor.e-dani.com/homelab/libreplay-web:sha-140431d86db3@sha256:f21e6e5212273f1ec22001b169e5b6324c0a02fd2b706421709dbb7ac9368192`; worker `harbor.e-dani.com/homelab/libreplay-web:worker-sha-140431d86db3@sha256:6fec50512d5e7578e540beef0351ce6cf8c7d627ac59dd9f1b4f00b60531d122`.
- [x] GitOps rollout deploys the runtime images plus the Redis no-eviction safety precondition. Evidence: GitOps commit `9e807a7 Deploy media moderation reconcile precondition`; GitOps CI `27913224329` completed `success`; Argo `libreplay` is `Synced|Healthy|9e807a7bff5f0b06989cd59a6f11c3361f22328a|Succeeded`; web/worker/redis rollouts passed; `k8s/manifest.yaml` Redis command is `[redis-server, --appendonly, "yes", --maxmemory-policy, noeviction]`.
- [x] Redis eviction policy is pinned at runtime. Evidence: `redis-cli CONFIG GET maxmemory-policy` returned `noeviction`, so rate-limit/queue keys are never silently evicted under memory pressure; the DB-driven reconcile loop is the safety net if Redis queue state is nonetheless lost on restart.
- [x] Runtime health and the reconcile loop are healthy. Evidence: `/api/health/deps` returned `ok:true` with authEmail/mediaModeration queues all `0`; worker logs from both pods show `media reconcile loop started` and `scanned=0 reEnqueued=0 skipped=0 failed=0`; focused LAN Playwright `e2e/33-payments-media.auth.spec.ts --project=member` returned `5 passed`.
- [blocked] Media backup/restore rehearsal is still not implemented. Evidence: the RHO checklist keeps the backup CronJobs and non-destructive restore rehearsal items `[ ]`; no Postgres/MinIO/Meili/Redis backup CronJob exists yet and no restore has been rehearsed. This precondition only proves Redis-ephemeral safety for media moderation; it is not backup or DR.
- [blocked] Scoped MinIO/PG backup credentials remain a separate DevOps/Security pass. Evidence: `security.report.md` decision `APPROVE_WITH_CONDITIONS` forbids MinIO root and treats the PG superuser as a temporary LAN-only exception; least-privilege backup-writer identities are not yet provisioned.
- [blocked] Non-destructive restore rehearsal remains open. Evidence: an independent verifier/auditor has not yet proven a full backup/DR result with at least one non-destructive runtime restore for Postgres/MinIO/Meili/Redis; the RHO Verifier/Auditor item is still `[ ]`.

## 2026-06-21 PMO Iteration - I18N Residual Label Coverage Deploy

- [x] Source externalizes the residual creator/group labels and is committed. Evidence: source commit `fb4847f fix(i18n): externalize creator and group labels`; source CI `27910647787` completed `success`; local build and typecheck passed.
- [x] 5-locale catalog parity is complete and guarded. Evidence: `apps/web/src/messages/{es,en,fr,de,pt}.json` each carry `985` string leaves with `0` missing, `0` extra, `0` empty and `0` placeholder mismatch; new guard `apps/web/src/i18n/catalog-parity.test.ts` returned `11/11` PASS.
- [x] Release image was published. Evidence: Release Image `27910863823` completed `success` for `sha-fb4847f77703`; web `harbor.e-dani.com/homelab/libreplay-web:sha-fb4847f77703@sha256:dbcb13fe9a469b4ad7f4e8d69fa52c6af3db1e9f3578a56a372b32a0dced1f44`; worker `harbor.e-dani.com/homelab/libreplay-web:worker-sha-fb4847f77703@sha256:e7454b50143c58f29cba05473e3f368c6d2cbdb7846bc0eb6a31f62ec1bccbe6`.
- [x] GitOps rollout is complete. Evidence: GitOps commit `cb51b7a Deploy i18n label coverage image`; GitOps CI `27911093094` completed `success`; Argo `libreplay` is `Synced|Healthy|cb51b7aa97da7ebe3d94d6494a61e2f8ae6a743a|Succeeded`; web/worker rollout status passed with pods running and zero restarts.
- [x] Runtime locale and regression checks passed. Evidence: `/api/health/deps` returned `ok:true` with authEmail/mediaModeration queues all `0`; live `/es`, `/en`, `/fr`, `/de`, `/pt` all returned HTTP `200`; focused LAN Playwright `e2e/12-creators-map-settings.auth.spec.ts` and `e2e/10-clubs-groups.auth.spec.ts` returned `7 passed`.
- [blocked] Production language quality sign-off remains incomplete. Evidence: technical label coverage is green, but production launch still needs human legal/adult-context linguistic QA for all non-Spanish locales.

## 2026-06-21 PMO Iteration - Process-Level HA Baseline

- [x] Research/Architecture/Security approved a safe stateless-only HA scope. Evidence: RHO checklist `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-ops-ha-01/RHO-CHECKLIST.md`; Researcher found only one eligible `lan+amd64` node and no PDB/HPA/NetworkPolicy; Architect approved `APPROVE_IMPLEMENTATION` for web/worker process-level HA only; Security approved no-NetworkPolicy scope.
- [x] Root Argo is configured not to fight HPA-managed replicas. Evidence: root GitOps commit `01a7c73 fix(argocd): let libreplay hpa own replicas`; live `Application/libreplay` includes `RespectIgnoreDifferences=true` plus `ignoreDifferences` entries for `Deployment/libreplay-web` and `Deployment/libreplay-worker` `/spec/replicas`; root `Application/root` is `Synced|Healthy|01a7c73`.
- [x] App GitOps process-level HA is deployed. Evidence: app GitOps commit `ac56634 feat(ops): add libreplay process ha baseline`; GitOps CI `27910056262` passed no-latest and manifest render/kubeconform; Argo `libreplay` is `Synced|Healthy|ac56634`.
- [x] Runtime web/worker HA objects are live and healthy. Evidence: `Deployment/libreplay-web` and `Deployment/libreplay-worker` are `2/2`; PDBs `libreplay-web` and `libreplay-worker` have `minAvailable=1` and `allowed disruptions=1`; HPAs target `cpu: 11%/70%` and `cpu: 2%/70%` with `min=2`, `max=4`, `replicas=2`; `kubectl top pods -n libreplay` reports metrics for all web/worker pods.
- [x] Runtime regression passed after rollout. Evidence: `/api/health/deps` returned `ok:true` for postgres, redis, queues, minio and meilisearch; focused LAN Playwright `e2e/33-payments-media.auth.spec.ts --project=member` returned `5 passed (7.5s)`; 10-minute web/worker log greps for `error|exception|failed|p1001|readablestream|crash|oom` returned no matches.
- [blocked] Node-level HA remains blocked. Evidence: only node `ubuntu` is both `topology=lan` and `kubernetes.io/arch=amd64`; both replicas land on that node. Real node HA requires a second eligible node or multi-arch images and scheduling changes.
- [blocked] Stateful HA/DR remain blocked. Evidence: Postgres, Redis, Meili and MinIO remain single-instance/RWO; production still needs stateful operators/clustering, backup/restore rehearsal and DR runbook.

## 2026-06-21 PMO Iteration - Media Delivery Scaffold Gate

- [x] Current media delivery inventory and decision are complete. Evidence: RHO checklist `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-media-delivery-01/RHO-CHECKLIST.md`; Researcher `PASS` mapped original streams, image variants, MP4, HLS, upload/presign and report routes; Architect approved `APPROVE_SCAFFOLD_ONLY`; Security approved `ALLOW_SCAFFOLD_ONLY` and prohibited wiring AWS `presignDownload`.
- [x] Source scaffold is implemented without live signed-CDN activation. Evidence: source commit `ed6e4ad Add fail-closed media delivery scaffold` adds `MEDIA_DELIVERY_MODE=proxy|signed-cdn`, `MEDIA_CDN_BASE_URL`, `MEDIA_SIGNING_KEY`, `MEDIA_SIGNED_URL_TTL_SECONDS`, `resolveDelivery`, no-leak tests and route fail-closed `503 MEDIA_DELIVERY_UNAVAILABLE`; `signed-cdn` is a throwing stub and `presignDownload` remains unused.
- [x] Source validation passed. Evidence: local PMO checks passed `@libreplay/config` `35`, `@libreplay/media` `30`, media route tests `15`, `pnpm typecheck`, `git diff --check`, local Docker target `runner`; source CI run `27905056358` completed `success` after rerun, with initial Docker smoke `exit 139` reproduced as non-deterministic because local Docker build and rerun both passed.
- [x] Release image and GitOps rollout passed. Evidence: Release Image `27905404183` completed `success` for `sha-ed6e4ad14ef0`; web digest `sha256:cf0b8c08d2699ba1dc60499914c92f05501dfb0650cdfa9bb186d1e6cd49c6e5`; worker digest `sha256:3c8236f27fec56f4e86018fadc95438f09626a24b58fe05b2966211f21f1cb61`; GitOps commit `416bfc2 Deploy media delivery scaffold image`; GitOps CI `27905674075` completed `success`.
- [x] Runtime is reconciled and media regression passed. Evidence: Argo `libreplay` is `Synced|Healthy|416bfc29442c7d438069a988efee1eebf49f80eb|Succeeded`; web and worker are `1/1` on the expected `ed6e4ad` digests with `0` restarts; `/api/health/deps` returned `ok:true`; focused LAN media/payment Playwright returned `5 passed (8.0s)`; 10-minute web/worker log greps for media/runtime errors returned no matches.
- [blocked] Live CDN/signed media delivery remains blocked. Evidence: `MEDIA_DELIVERY_MODE` defaults to `proxy`, LAN does not set CDN secrets, staging runtime overlay sets `MEDIA_DELIVERY_MODE: proxy`, and no `MEDIA_CDN_BASE_URL`, `MEDIA_SIGNING_KEY` or `signed-cdn` appears in rendered GitOps. Real activation still needs provider/signing semantics, custom CDN domain, Vault/ESO signing key, short-TTL/revocation policy and no-leak live E2E.

## 2026-06-21 PMO Iteration - SEC-04 Authz/BOLA Matrix Closure

- [x] SEC-04 route inventory is complete for the named residual BOLA surfaces. Evidence: RHO checklist `/home/dibanez/k8s/.rho/claude-subagents/libreplay/prod-sec-04-authz-matrix/RHO-CHECKLIST.md`; Claude CLI reviewer reports cover albums/map, creator account, subscription/PPV, paid-content/media, admin console, admin actions and user report routes.
- [x] Albums, media grants, map/location shares and creator payment/subscription flows passed independent review. Evidence: `security-review-albums-map-context.report.md` PASS; `security-review-creator-account-mini.stdin.report.md` PASS; `security-review-subscription-ppv-mini.report.md` PASS; `security-review-paidcontent-media-mini.report.md` PASS.
- [x] Admin/moderation object scopes passed independent review. Evidence: `security-review-admin-console-short-tools.report.md` PASS; `security-review-admin-actions-mini.report.md` PASS; `security-review-user-reports-mini.report.md` PASS.
- [x] Focused security verification passed. Evidence: `security-verifier-focused-tests.report.md` PASS with `55` tests passed across creator/payment/media permission suites; `security-verifier-admin-tests.report.md` PASS with `39` tests passed across admin/moderation/security IDOR suites.
- [x] No implementation lane was opened because no actionable BOLA/IDOR gap was found. Evidence: source repo `/home/dibanez/k8s/libreplay` remained `## main...origin/main`; GitOps repo remained clean before PMO docs edits.
- [blocked] Production security remains incomplete outside SEC-04. Evidence: real OAuth/SMTP/payment/media-moderation providers, public trusted-proxy proof, secrets rotation, compliance/legal sign-off and operational security controls remain open.

## 2026-06-21 PMO Iteration - I18N Runtime Rollout

- [x] Source i18n implementation is committed and pushed. Evidence: source commit `5f9ec80 Internationalize web UI across supported locales`; source CI `27902163809` completed success with typecheck, unit tests, web build and Docker build smoke.
- [x] 5-locale catalog integrity is verified. Evidence: `apps/web/src/messages/{es,en,fr,de,pt}.json` all have `946` leaf keys with missing `0` and extra `0`; referenced-key scan found `677` static refs with missing `0`; hardcoded Spanish UI scan over `apps/web/src/app/[locale]` and `apps/web/src/components` returned no visible UI hits.
- [x] Release image for i18n was published. Evidence: Release Image `27902421808` completed success for source `5f9ec801fc3f`; web `sha-5f9ec801fc3f@sha256:693d56684c163c0b85734c9f0a100ff2248cc6876d5f052d6eb7b3913f2012c9`; worker `worker-sha-5f9ec801fc3f@sha256:95961909fbe77cdcc82bba5fa75e2d770de791fb1ca14333501ab6168bd64491`.
- [x] GitOps rollout is complete. Evidence: GitOps commit `387591a deploy libreplay i18n image` changes only the web and worker image refs in `k8s/manifest.yaml`; local `kubectl kustomize` checks for `k8s`, `staging`, `staging/overlays/runtime` and `production` passed; no-`:latest` check passed; GitOps CI `27902619232` completed success.
- [x] Runtime is reconciled and healthy. Evidence: Argo `libreplay` is `Synced|Healthy|387591af6aeb936d8304a3b685344d6ded240b9f|Succeeded`; web and worker rollouts completed `1/1` on the expected digests; `/api/health/deps` returned postgres, redis, queues, minio and meilisearch `ok:true`.
- [x] Runtime localized smoke passed. Evidence: LAN Playwright smoke visited landing, login, register, forgot-password, terms, privacy and community-rules for `es/en/fr/de/pt`; all `35` checks returned status `200` and found expected catalog text; 10-minute web/worker log greps found no i18n/runtime error matches.
- [blocked] Production language quality sign-off remains incomplete. Evidence: technical i18n rollout is green, but production launch still needs human legal/adult-context linguistic QA for all non-Spanish locales.

## 2026-06-21 PMO Iteration - Media Report Legal Hold

- [x] Source report and moderation flow exists. Evidence: source commit `f6ad763` adds `POST /api/media/[id]/report`, creates `Report` and `ModerationCase` atomically with `mediaAssetId`, rate-limits before body/lookup/write, checks `canViewMediaAsset`, applies DB legal hold on `REMOVE_CONTENT`, blocks media `RESTORE_CONTENT`, and adds report/case `mediaAssetId,status` indexes.
- [x] Source validation and independent review passed. Evidence: local web security tests `32 passed`, media retention tests `7 passed`, `pnpm typecheck`, Prisma validate and fresh Postgres `prisma migrate deploy` smoke passed; source CI `27889698410`; Mencius/Nash/Erdos verifier passes.
- [x] Release and GitOps deploy passed. Evidence: Release Image `27889835533`, GitOps commit `7204aa6`, GitOps CI `27890068627`, web `sha-f6ad7631d275@sha256:3a39aefe93a4d6c6a7def73a7ae86dc756d496ecc948ebe9438a3b9032890b8d`, worker `worker-sha-f6ad7631d275@sha256:c10548aefc111a8e0d4657ba777eecd5b5c36ba224bab57784ce0a6c72f3ffea`, tools `tools-sha-f6ad7631d275@sha256:6d2346eb59f39f3d193b36ee59e86d65e852962ca641dcd1c4a4fb1bd887db72`.
- [x] Runtime migration and regression checks passed. Evidence: Argo `Synced|Healthy|7204aa656413bef80290d72a66e9bce208916c46|Succeeded`; `Job/libreplay-db-migrate-f6ad763` completed; DB indexes `Report_mediaAssetId_status_idx` and `ModerationCase_mediaAssetId_status_idx` exist; `/api/health/deps` is healthy; focused media/payment Playwright returned `5 passed (7.4s)`; recent web/worker log greps returned no matches.
- [blocked] Full safety workflow remains incomplete. Evidence: moderator UI for media legal hold release/restore, real media/CSAM providers, law-enforcement/NCMEC-equivalent escalation policy and legal/compliance sign-off remain open.

## 2026-06-21 PMO Iteration - Media Retention Lifecycle

- [x] Source DB lifecycle state exists. Evidence: source commit `50e1d14` adds `MediaRetentionClass`, `MediaPurgeState`, `deletedAt`, `purgeAfter`, `purgeState`, `purgedAt`, legal-hold fields and migration `20260621003500_media_retention_lifecycle`; source commit `7da4345` adds read-only S3-to-DB orphan inventory and a fail-closed physical-purge contract.
- [x] Retention planner is default-safe. Evidence: `packages/jobs/src/handlers/media-retention.ts` defaults to dry-run, CLI requires `--execute` or `MEDIA_RETENTION_EXECUTE=1`, summaries omit object keys, and physical S3 delete is not invoked.
- [x] Planner protects live/legal references. Evidence: relation blockers cover posts, albums, messages, profile/event/date/blog covers, content participants, PPV, paid content, verification evidence, consent documents, open reports and moderation cases; intrinsic blockers cover demo media, `verification/` object keys, legal holds and `csam_match`.
- [x] Source CI and release passed. Evidence: local `pnpm typecheck`, focused job/media/web tests, source CI `27888886986`, and Release Image `27889021579` completed `success` for `50e1d14`.
- [x] Runtime migration/rollout validated. Evidence: GitOps commit `02c806c`, GitOps CI `27889214576`, Argo `Synced|Healthy|02c806c4df54bb3a1e493cd9dbf6fd8ad3bea694|k8s`, migration Job `libreplay-db-migrate-50e1d14` completed, web/worker images are on `50e1d14` digests, `/api/health/deps` is healthy, retention dry-run returned `dryRun=true` with `20` stale pending candidates and no object keys, DB status summary remains `PENDING_UPLOAD|ACTIVE|46` and `APPROVED|ACTIVE|193`, focused media/payment E2E returned `5 passed (7.9s)`, and 10-minute web/worker log greps returned no matches.
- [blocked] Full production media lifecycle remains incomplete. Evidence: read-only orphan inventory is now runtime-validated, but physical S3 purge execution is intentionally blocked pending legal/backup/MinIO-versioning approval and Security/PMO signoff; CDN/signed delivery, backup/restore, real CSAM/media moderation and worker scaling remain open.

## 2026-06-21 PMO Iteration - Media Storage Lifecycle Gate

- [x] MinIO media bucket versioning is deployed through GitOps. Evidence: GitOps commit `9848ef4 feat(media): add minio lifecycle policy gate` adds `Job/libreplay-minio-policy-20260621`; Argo `libreplay` is `Synced|Healthy|9848ef458cca0c6c2c0645cea4bf0e292b8a9346|k8s`; live Job completed successfully and logged `versioning is enabled`.
- [x] Conservative lifecycle is deployed without deleting current active media by age. Evidence: live MinIO lifecycle rule `d8rj0au5vkbs7hha9tkg` has delete-marker expiration enabled, noncurrent expiration after `30` days and `KEEP VERSIONS=2`; `scripts/check-libreplay-media-storage-policy.sh --static` rejects `--expire-days` for this gate.
- [x] Staging runtime will inherit the policy when it is later enabled. Evidence: `scripts/check-libreplay-media-storage-policy.sh --static` renders `staging/overlays/runtime` and confirms `Job/libreplay-minio-policy-20260621` in `libreplay-staging` targeting `libreplay-media-staging`.
- [x] Runtime media flows still pass after lifecycle. Evidence: focused LAN media/payment E2E returned `5 passed (7.1s)`, `/api/health/deps` returned `ok:true`, and 5-minute web/worker error scans returned no matches.
- [blocked] Full production media lifecycle remains incomplete. Evidence: CDN/signed delivery, approved physical purge execution, production media host, backup/restore rehearsal, real CSAM/media moderation providers and worker scaling remain open; DB-backed retention fields and read-only orphan inventory are present.

## 2026-06-21 PMO Iteration - Staging Secrets Evidence Bootstrap

- [x] Non-secret evidence gate exists. Evidence: `scripts/check-libreplay-staging-secret-evidence.sh` validates the exact Google callback URI, Facebook callback URI, SMTP sender identity and controlled mailbox shape without printing OAuth, SMTP or mailbox values.
- [x] Vault bootstrap refuses write mode without evidence metadata. Evidence: `scripts/bootstrap-libreplay-staging-secrets.sh --write` now calls `scripts/check-libreplay-staging-secret-evidence.sh --strict --vault-write` before `vault kv put`.
- [x] SMTP bootstrap posture matches staging config. Evidence: the bootstrap rejects `SMTP_PORT` values other than `465` while `SMTP_SECURE=true`, and rejects placeholder/local SMTP hosts.
- [x] Source-side app config now mirrors staging provider hardening. Evidence: source config rejects local/LAN/example URLs, malformed Google/Meta IDs, placeholder provider secrets, missing SMTP auth and missing `SMTP_SECURE=true` in staging/production; focused env tests returned `24 passed`.
- [x] GitHub staging smoke now requires mailbox delivery confirmation. Evidence: source workflow reads `PW_STAGING_AUTH_EMAIL_PLUS_CONFIRMED`, source preflight checks both required secret names and strict mode blocks on both missing secrets.
- [x] Runbook key inventory is consistent. Evidence: `docs/libreplay-staging-auth-runbook.md` now lists all 19 required staging secret keys including `METRICS_BEARER_TOKEN`.
- [x] Source provider hardening is deployed to LAN. Evidence: source head `707a837`, source CI run `27887682406`, Release Image run `27887825746`, GitOps commit `69e7504`, GitOps CI run `27888042812`, Argo `libreplay` `Synced|Healthy|69e7504514ea1f103c0f6b42084e813aa73f3660|k8s`, web/worker `1/1` on the `707a8375192c` digest-pinned images, `/api/health/deps` healthy, full LAN Playwright `97 passed (1.6m)`, and 10-minute web/worker error scans returned no matches.
- [blocked] Full staging real-auth remains blocked. Evidence: provider values, callback evidence, SMTP sender evidence, `PW_STAGING_AUTH_EMAIL` and `PW_STAGING_AUTH_EMAIL_PLUS_CONFIRMED` have not been supplied; `ExternalSecret/libreplay-secrets` remains blocked until Vault `secret/libreplay/staging` is written.

## 2026-06-21 PMO Iteration - Staging GitHub Environment Gate

- [x] Source preflight script exists. Evidence: source commit `9d6f1e9 feat(auth): add staging github env preflight` adds `scripts/check-libreplay-staging-github-env.sh`, which checks `.github/workflows/staging-real-auth.yml`, `environment: staging`, and environment secret names only.
- [x] GitHub environment `staging` exists. Evidence: `scripts/check-libreplay-staging-github-env.sh --ensure` created `pocharlies-org/libreplay` environment `staging`; `gh api repos/pocharlies-org/libreplay/environments` now lists `name=staging`.
- [x] The gate fails closed without staging mailbox secrets. Evidence: `scripts/check-libreplay-staging-github-env.sh --strict` verifies the workflow/environment and reports missing `PW_STAGING_AUTH_EMAIL` and `PW_STAGING_AUTH_EMAIL_PLUS_CONFIRMED`.
- [x] Independent specialist passes agree on the blocker boundary. Evidence: Auth/Email subagent `Jason` confirmed code/contract/smoke are implemented and blocked by Vault/provider/mailbox secrets; DevOps/Staging subagent `Lovelace` confirmed Argo staging is contract-only, DNS/TLS and `harbor-pull` are OK, and `ExternalSecret/libreplay-secrets` fails because `secret/libreplay/staging` does not exist.
- [blocked] Full staging real-auth workflow remains blocked. Evidence: GitHub environment secrets `PW_STAGING_AUTH_EMAIL` and `PW_STAGING_AUTH_EMAIL_PLUS_CONFIRMED` are absent; Vault `secret/libreplay/staging` is still absent; `ExternalSecret/libreplay-secrets` remains `Ready=False SecretSyncedError`; no Google/Meta/SMTP provider smoke can run yet.

## 2026-06-20 PMO Iteration - Mobile PWA Readiness Gate

- [x] Frontend/mobile audit completed before implementation. Evidence: subagent `Epicurus` reported missing manifest, icons, explicit PWA metadata, `viewport-fit`, offline strategy, automated a11y, upload/camera breadth and deeper mobile navigation coverage.
- [x] Source implementation is committed and pushed. Evidence: source commit `be5555f feat: add mobile pwa readiness` adds `/manifest.webmanifest`, PWA icons, Apple/theme/viewport metadata, safe-area CSS, global focus-visible styling, and mobile Playwright spec `e2e/36-pwa-mobile.mobile.spec.ts`; audit record lives in `docs/prod-mobile-pwa-01-rho-audit-2026-06-20.md`.
- [x] Source validation and official release passed. Evidence: local typecheck, web build, local dev PWA E2E, built-app PWA E2E and `git diff --check` passed; source CI run `27885549692` completed `success`; Release Image run `27885849278` completed `success` for `sha-be5555f9dc0f` with web digest `sha256:97308d307d17ba51ad0e9b1b39246b4621572d92fa0762199350407402f2f8e4` and worker digest `sha256:b916acfa1298d4bec0910c704485c527f7ab15aac13b96dc706d67207d5145b2`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `ccfa39c fix: deploy mobile pwa readiness` changes only web/worker image refs and rollout annotations; server-side dry-run, Kustomize client dry-run and no-`:latest` checks passed; GitOps CI run `27886232754` completed `success`.
- [x] Runtime is reconciled on expected images. Evidence: Argo `libreplay` is `Synced|Healthy|ccfa39c37a7e7b1fd280a8436a062c06e15e7a69|k8s`; Argo `libreplay-staging` remains expected `Synced|Degraded|ccfa39c37a7e7b1fd280a8436a062c06e15e7a69|staging`; web is `1/1` on `sha-be5555f9dc0f@sha256:97308d307d17ba51ad0e9b1b39246b4621572d92fa0762199350407402f2f8e4`; worker is `1/1` on `worker-sha-be5555f9dc0f@sha256:b916acfa1298d4bec0910c704485c527f7ab15aac13b96dc706d67207d5145b2`.
- [x] Runtime PWA, health and regression gates passed. Evidence: `/api/health/deps` returned `ok:true` for postgres, redis, queues, minio and meilisearch; `/manifest.webmanifest` returns `SauvagePlay`, `start_url=/es`, `display=standalone` and the three expected icons; focused LAN mobile PWA E2E returned `2 passed (3.4s)`; full LAN Playwright returned `97 passed (2.3m)`.
- [x] Post-rollout logs are clean. Evidence: 10-minute web and worker greps for `p1001|readablestream|already closed|error|exception|failed|unhandled` returned no matches.
- [blocked] Full mobile product readiness remains blocked. Evidence: installability, icons, metadata, safe-area and focus visibility are now deployed, but production still needs a privacy-aware offline/cache design, automated axe/a11y, touch gesture polish, upload/camera progress UX, real-device Android/iOS browser validation, mobile performance screenshots/budget and product navigation decisions for deep areas.

## 2026-06-20 PMO Iteration - Prototype LLM Mock Endpoint Fail-closed Gate

- [x] Security/Backend audit completed before implementation. Evidence: subagent `019ee667-a534-73a1-ac7a-527ec3c2cba3` audited `/api/llm/stt`, `/api/llm/tts`, `/api/llm/interview`, callers, env controls, rate limiting and CSRF posture, and found those prototype mock surfaces were not governed by `ENABLE_MOCK_LLM`/`DEPLOYMENT_MODE`.
- [x] Source implementation is committed and pushed. Evidence: source commit `ee75752 fix: guard prototype llm routes outside demo` adds shared `mockLLMRouteUnavailableResponse`, gates `/api/llm/interview`, `/api/llm/tts` and `/api/llm/stt`, bounds interview/STT payloads, handles unavailable interview responses in the client, adds direct route tests and records RHO audit `docs/prod-safe-02-llm-mock-endpoints-rho-audit-2026-06-20.md`.
- [x] Source validation passed locally and remotely. Evidence: local `@libreplay/web` route posture tests passed `10/10`, `@libreplay/config` env tests passed `16/16`, `@libreplay/media` provider tests passed `5/5`, `@libreplay/web` typecheck and `git diff --check` passed; source CI run `27881027123` completed `success` with typecheck, unit tests, web build and Docker smoke.
- [x] Official release digests were published. Evidence: Release Image run `27881292534` completed `success` for source `ee757524d26c`; web digest `sha256:2a488953569e0610cda6fe3a4b1a478084b8849ffa8593b49645df15b5957783`; worker digest `sha256:3aa976f1089b371eb03e577fe46682cff0c1deb39ad6455bf57474786d116ee1`; tools digest `sha256:f348feee6764919e229ac4b076657f3bb27c8bce2ce86d2a91035c922e3a205c`; seed digest `sha256:fcde52d3f2f8d0d0f43e21b9fc6b66e38663a4c510edc37ffc3851895310f516`.
- [x] GitOps deploy is complete. Evidence: functional GitOps commit `4b29384 feat: deploy libreplay llm mock guard` changes only web/worker image refs and rollout annotations; GitOps CI run `27881647463` completed `success`; Argo `libreplay` is `Synced|Healthy|4b29384db2589e686aaca2381decd6bcf69d2df3|k8s`; Argo `libreplay-staging` is still expected `Synced|Degraded|4b29384db2589e686aaca2381decd6bcf69d2df3|staging` because staging secrets are not materialized.
- [x] Runtime health and regression gates passed after deploy. Evidence: web runs `sha-ee757524d26c@sha256:2a488953569e0610cda6fe3a4b1a478084b8849ffa8593b49645df15b5957783`; worker runs `worker-sha-ee757524d26c@sha256:3aa976f1089b371eb03e577fe46682cff0c1deb39ad6455bf57474786d116ee1`; `/api/health/deps` returned `200` with postgres/redis/minio/meilisearch `ok:true` and authEmail/mediaModeration queues all `0`; full LAN Playwright returned `95 passed (2.5m)`; post-E2E web/worker log greps for `p1001|readablestream|already closed|error|exception|failed|SAFETY_PROVIDER_NOT_CONFIGURED` returned no matches.
- [blocked] Real LLM/STT/TTS providers remain missing. Evidence: this meta prevents prototype mocks from leaking into staging/production, but real provider contracts, secrets, abuse controls and UX policy for sensitive interview questions before onboarding/verification remain open; `/api/landing/oracle` is a separate public LLM-like surface queued for audit.

## 2026-06-20 PMO Iteration - Safety Provider Mock Fail-closed Gate

- [x] Source implementation is committed and pushed. Evidence: source commit `9899af4 fix: fail closed safety providers outside demo` adds guarded `@libreplay/media` safety provider selectors, `SAFETY_PROVIDER_NOT_CONFIGURED`, staging/production env rejection for age/face/CSAM/media moderation/LLM mocks, HTTP 503 mapping for verification and AI profile routes, and RHO audit `docs/prod-safe-01-provider-mock-fail-closed-rho-audit-2026-06-20.md`.
- [x] Source validation passed locally and remotely. Evidence: focused local media/config/jobs/web tests passed (`9`, `16`, `4`, `18` tests respectively), focused package typechecks passed, `git diff --check` passed, and source CI run `27880061779` completed `success` with typecheck, unit tests, web build and Docker smoke.
- [x] Official release digests were published. Evidence: Release Image run `27880290417` completed `success` for source `9899af48ec1e`; web digest `sha256:3727df8f09d2e3f5330bdb28ddd606840bf6111fceb605c27cc17fc2884868e0`; worker digest `sha256:07df651838e215a8c0cab7fe72d5877f240d14d932654112190346f4d5602f07`; tools digest `sha256:712e95ce642be46bd4a6e53289abb61cdc7ade136282bd9545a7e05ba83aa4bd`; seed digest `sha256:0743c41d3ff163ce353245135f9a0c92ad57e7aa21643f7f2b2a1a63c2f81a8f`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `99cf561 feat: deploy libreplay safety provider guard`; GitOps CI run `27880590012` completed `success`; Argo `libreplay` is `Synced|Healthy|99cf5619dff021e790d45e8b3aef53b70481d84c|k8s`; web runs `sha-9899af48ec1e@sha256:3727df8f09d2e3f5330bdb28ddd606840bf6111fceb605c27cc17fc2884868e0`; worker runs `worker-sha-9899af48ec1e@sha256:07df651838e215a8c0cab7fe72d5877f240d14d932654112190346f4d5602f07`.
- [x] Runtime health and regression gates passed after deploy. Evidence: `/api/health/deps` returned `200` with postgres/redis/minio/meilisearch `ok:true` and authEmail/mediaModeration queues all `0`; full LAN Playwright returned `95 passed (2.3m)`; post-E2E web/worker log greps for `p1001|readablestream|already closed|error|exception|failed|SAFETY_PROVIDER_NOT_CONFIGURED` returned no matches.
- [blocked] Real providers remain missing. Evidence: age verification, face liveness, CSAM, media moderation and LLM mocks are now blocked outside demo modes; production still needs real provider contracts/secrets before those workflows can be live.

## 2026-06-20 PMO Iteration - Browser Double-submit CSRF Gate

- [x] Source implementation is committed and pushed. Evidence: source commit `ebaf65c feat: add double-submit csrf for api mutations` adds `/api/csrf`, `libreplay.csrf`, `x-libreplay-csrf`, shared `apiFetch`, migrated client mutators and E2E CSRF helpers.
- [x] Source validation and official release passed. Evidence: source CI run `27866727135` completed `success`; Release Image run `27866884997` completed `success` for `sha-ebaf65ce0676`; web digest `sha256:51c24aaade3438245648889a004246934e45d7d7576e81ed1c2bead334c8645b`; worker digest `sha256:6e8a69bdd6d92f4910f6d9f4d68001580d14c78381b3803672e1acfbfd30aeb2`.
- [x] GitOps deploy is complete and minimal. Evidence: commit `b8b4b38 feat: deploy libreplay csrf gate` changes only web/worker image refs and rollout annotations in `k8s/manifest.yaml`; `kubectl apply --dry-run=server -f k8s/manifest.yaml`, `kubectl kustomize k8s` and `git diff --check` passed; GitOps CI run `27867139549` completed `success`.
- [x] Independent DevOps verification passed. Evidence: Plato confirmed release tags/digests, `b8b4b38` on `deploy/prod`, only four manifest line changes, no tools/seed/migration job needed and YAML valid.
- [x] Runtime is reconciled on expected images. Evidence: Argo `libreplay` is `Synced|Healthy|b8b4b38906926fc8633f584acb93a5f3c02321ca`; web `1/1` on `sha-ebaf65ce0676@sha256:51c24aaade3438245648889a004246934e45d7d7576e81ed1c2bead334c8645b`; worker `1/1` on `worker-sha-ebaf65ce0676@sha256:6e8a69bdd6d92f4910f6d9f4d68001580d14c78381b3803672e1acfbfd30aeb2`.
- [x] Runtime CSRF, health and regression gates passed. Evidence: `/api/csrf` returns `200` with `libreplay.csrf` cookie/token; cross-site `POST /api/posts` returns `403 INVALID_ORIGIN`; same-origin unauthenticated POST returns `307`; `/api/health/deps` and `/api/metrics` return `200`; full LAN Playwright returned `95 passed (1.6m)`; post-E2E web/worker log greps for `p1001|readablestream|already closed|error|exception|failed` returned no matches.
- [blocked] Full public security remains blocked outside this meta. Evidence: staging/prod real provider secrets are absent, public trusted client-IP runtime proof is blocked by staging secrets, real PSP webhook signatures are not integrated, and secrets rotation/compliance controls remain open.

## 2026-06-20 PMO Iteration - Runtime Observability Metrics Gate

- [x] SRE audit identified the smallest safe production-readiness increment. Evidence: Feynman reported live `/api/health/deps` was healthy but readiness still used shallow `/api/health`, `/api/metrics` was not deployed, the Service lacked a scrape label and metrics needed protection before public staging/prod exposure.
- [x] Source observability implementation is committed and validated. Evidence: source commits `bc89e4c`, `dabe952` and `2aba268` add shared dependency checks, `/api/metrics`, metrics auth policy outside LAN/dev/test and stabilized tests; local focused tests, web/config typecheck, full web tests, config tests, web build and `git diff --check` passed.
- [x] Source CI and release image passed. Evidence: final source CI run `27864613745` completed `success`; Release Image run `27864764203` completed `success` for `sha-2aba2684d4a4`; published web digest `sha256:554f61bc5fb45324b531e9864686075bc16e2277b2ac7594125e32d7b8d08657` and worker digest `sha256:8955ceb44849e2f386cd1490e68a5c0e04db68982a55a8e1f27c68a8dfaa7b8e`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `ceb8e27 feat: deploy libreplay runtime observability` deployed source `2aba268`; follow-up `8f9117a fix: scope libreplay metrics bearer to staging` removed the base bearer reference after VictoriaMetrics rejected a missing LAN secret key; GitOps CI runs `27865066419` and `27865147747` completed `success`.
- [x] Runtime probes and scrape config are correct. Evidence: Argo `libreplay` is `Synced|Healthy|8f9117a9af22d7df5521c6f4f36695d9b9ce273d`; web/worker rollouts completed; web readiness is `/api/health/deps` timeout `5`, liveness is `/api/health` timeout `3`, Service label/selector is `app=libreplay-web`, and `VMServiceScrape/libreplay-web` is `updateStatus=operational` with `Applied=True`.
- [x] Runtime metrics and dependency health are live. Evidence: `/api/health/deps` returned `200` with postgres/redis/queues/minio/meilisearch `ok:true`; `/api/metrics` returned `200 text/plain` and emitted `libreplay_dependency_up`, `libreplay_dependency_latency_ms`, `libreplay_queue_jobs`, uptime and memory metrics without secret values.
- [x] Staging metric auth remains fail-closed. Evidence: base LAN `VMServiceScrape` has no bearer secret; `staging/overlays/runtime/patch-vmservicescrape.yaml` adds `bearerTokenSecret` using `METRICS_BEARER_TOKEN`; staging contract, runtime overlay and preflight require `METRICS_BEARER_TOKEN`; `scripts/check-libreplay-staging-runtime.sh` verifies bearer-token usage and required env secret refs.
- [x] Independent verifier pass completed. Evidence: Halley reported PASS for the corrected worktree, re-ran read-only render/check commands, confirmed base LAN scrape has no bearer, staging runtime adds bearer, Argo is healthy and `VMServiceScrape` is operational.
- [x] Recent runtime logs are clean for this rollout. Evidence: 10-minute web and worker greps for `p1001|readablestream|already closed|error|exception|failed` returned no matches.
- [x] Alert/dashboard follow-up is split out and closed below. Evidence: PROD-11B adds baseline VMRule alerts plus Grafana dashboard while preserving SLO/error-tracking/synthetic checks as separate production blockers.

## 2026-06-20 PMO Iteration - Runtime Alerts And Dashboard Gate

- [x] GitOps alert/dashboard resources are committed and pushed. Evidence: commit `ee2672a feat: add libreplay runtime alerts` adds `k8s/vmrule.yaml`, `k8s/grafana-dashboard.yaml` and kustomization entries; commit `a2b199d fix: prevent staging monitoring drift` patches staging runtime to target `namespace="libreplay-staging"` and disable inherited LAN dashboard import.
- [x] GitOps CI passed for both alert/dashboard revisions. Evidence: CI run `27865493738` passed for `ee2672a`; CI run `27865708701` passed for `a2b199dc3b9a543f96df0ffbca6dcf40e1f30e95`.
- [x] LAN runtime is reconciled on the corrected revision. Evidence: Argo `libreplay` reports `Synced|Healthy|a2b199dc3b9a543f96df0ffbca6dcf40e1f30e95|k8s`; `libreplay-staging` remains contract-only `Synced|Degraded|a2b199dc3b9a543f96df0ffbca6dcf40e1f30e95|staging`.
- [x] Live alert rule is accepted by VictoriaMetrics operator/VMAlert. Evidence: `VMRule/libreplay-runtime` in `monitoring` reports `updateStatus=operational` with condition `Applied=True:ConfigParsedAndApplied`.
- [x] VMAlert loaded the expected rules and healthy state is quiet. Evidence: VMAlert API group `libreplay.runtime` contains 8 rules (`LibrePlayMetricsScrapeMissing`, `LibrePlayDeploymentUnavailable`, `LibrePlayPostgresUnavailable`, `LibrePlayDependencyDown`, `LibrePlayDependencyLatencyHigh`, `LibrePlayQueueFailures`, `LibrePlayQueueBacklogHigh`, `LibrePlayWorkerOrWebRestarting`), all `state=inactive`, `health=ok`, `alerts=0`; `/api/v1/alerts` has no active `LibrePlay*` alerts.
- [x] Grafana dashboard import is live. Evidence: `ConfigMap/grafana-dashboard-libreplay-runtime` in `monitoring` has label `grafana_dashboard=1`; dashboard JSON parses with uid `libreplay-runtime`, title `LibrePlay Runtime`, 7 panels and `refresh=30s`.
- [x] Dashboard queries use live LibrePlay metric labels. Evidence: dashboard expressions reference `up`, `libreplay_dependency_up`, `libreplay_queue_jobs`, `libreplay_dependency_latency_ms`, `libreplay_process_memory_bytes` and web/worker restart metrics scoped to `namespace="libreplay"` and `service="libreplay-web"`.
- [x] Future staging runtime cannot inherit LAN monitoring selectors. Evidence: `scripts/check-libreplay-staging-runtime.sh` passes and rejects `namespace="libreplay"` plus `grafana_dashboard: "1"`; rendered staging runtime has VMRule expressions scoped to `namespace="libreplay-staging"`, VMServiceScrape bearer token auth and dashboard label `grafana_dashboard: "0"`.
- [blocked] Full production observability remains incomplete. Evidence: baseline metrics, scrape, alerts, dashboard, LAN synthetic checks and the first structured logging scaffold now exist, but production still needs full web log migration, external error tracking, alert fire/route rehearsal, full multi-window error-budget policy and recurrence/root-cause follow-up for any future DB or stream-close errors.

## 2026-06-20 PMO Iteration - Staging Secrets Intake Gate

- [x] Live staging blocker was revalidated. Evidence: `scripts/check-libreplay-staging-preflight.sh --strict` reports only `ExternalSecret/libreplay-secrets` `Ready=False|SecretSyncedError` and absent `Secret/libreplay-secrets`; `harbor-pull`, DNS and TLS remain OK.
- [x] Safe 1Password metadata discovery was performed without secret values. Evidence: OpenClaw/1Password status succeeded; no item named LibrePlay/SauvagePlay was found; adjacent candidates were inspected by item metadata and field labels only.
- [x] Staging secret intake now exists. Evidence: [libreplay-staging-secrets-intake.md](./libreplay-staging-secrets-intake.md) maps all 19 required Vault fields, source expectations, validation rules, blocked provider fields and a no-echo Vault push procedure.
- [blocked] Provider-specific credentials remain missing. Evidence: Google OAuth candidate metadata points to a Skirmshop-titled app, Facebook candidates are login items rather than proven app credentials, and SMTP-adjacent Resend/Mailrelay/Gmail metadata lacks LibrePlay sender/domain evidence.
- [blocked] Staging runtime workloads must remain unsynced. Evidence: `secret/libreplay/staging` is still absent/incomplete, so `staging/overlays/runtime` must not be promoted until ESO materializes the Secret and strict preflight passes.

## 2026-06-20 PMO Iteration - PSP Transaction Ledger Scaffold

- [x] Payments/PSP subagent completed a read-only audit. Evidence: Poincare verified no Stripe SDK/env exists, `PAYMENT_PROVIDER` remains `disabled|mock`, mock routes are LAN/test guarded, `/api/payments/webhook` fails closed and Stripe policy blocks adult/pay-per-view assumptions without written approval.
- [x] Provider-neutral payment ledger scaffold exists in source. Evidence: source worktree adds `PaymentTransactionKind` and `PaymentTransaction` with unique provider reference and webhook event keys in `packages/db/prisma/schema.prisma`, plus migration `20260620090000_payment_transaction_ledger`.
- [x] Web domain helper exists without activating a PSP. Evidence: source worktree adds `apps/web/src/lib/payment-transactions.ts`, which creates pending transactions and completes them only from provider webhook evidence while returning duplicate webhook events idempotently.
- [x] Payment fail-closed behavior remains intact. Evidence: `pnpm --filter @libreplay/media test -- src/__tests__/payment-provider.test.ts` passed `4/4`; `pnpm --filter @libreplay/config test -- src/__tests__/env.test.ts` passed `15/15`; web payment/middleware/ledger tests passed `9/9`; `PAYMENT_PROVIDER` still does not accept Stripe.
- [x] Source and release automation are complete for the ledger scaffold. Evidence: source commit `df16891 feat(payments): add transaction ledger scaffold`; source CI run `27863683843` completed `success`; corrected Release Image run `27863843341` completed `success` for `sha-df168914672d` after an initial manual dispatch failure without version input.
- [x] GitOps deploy is complete. Evidence: functional GitOps rollout commit `f65342d fix: deploy libreplay payment ledger scaffold`; GitOps CI run `27864058325` completed `success`; Argo `libreplay` synced healthy after the rollout; Argo `libreplay-staging` remains contract-only `Synced|Degraded` because `ExternalSecret/libreplay-secrets` is still missing provider data. Later docs-only commits may advance the Argo revision without changing workload images.
- [x] Runtime migration and images are applied. Evidence: job `libreplay-db-migrate-df16891` completed in `9s`; logs show migration `20260620090000_payment_transaction_ledger` successfully applied; DB query returned `"PaymentTransaction"`; `libreplay-web` runs `sha-df168914672d@sha256:c75d9ef07b1648e9f1c307d090c0777239bfecb81b7b0c8e8db442f6f44376c2`; `libreplay-worker` runs `worker-sha-df168914672d@sha256:91d9f9c62c78c47f9899d63b4d309b8ae67e70ed3cabd547015d7f2524b4b158`.
- [x] Runtime health and focused product gates passed after deploy. Evidence: `/api/health/deps` returned `200` with postgres/redis/minio/meilisearch `ok:true` and queues `0`; `POST /api/payments/webhook` returned `503 PAYMENT_PROVIDER_NOT_CONFIGURED`; focused LAN E2E for payment/media/demo-visuals/mobile visuals returned `10 passed (13.0s)`.
- [blocked] Stripe is not activated. Evidence: official Stripe restricted-business documentation lists adult services, pay-per-view adult features, adult live chat and mature sexual content as prohibited/restricted; LibrePlay needs written approval or an adult-friendly PSP decision before live payments.

## 2026-06-20 PMO Iteration - Stripe Sandbox PPV Gate

- [x] Source Stripe sandbox-only gate committed and pushed. Evidence: source commit `f3b9301 feat: add stripe sandbox payment gate`; source CI run `27884105851` completed `success` with Typecheck, Unit tests, Build web and Docker build smoke.
- [x] Release and LAN rollout completed without enabling Stripe. Evidence: Release Image run `27884407815` completed `success`; web image `sha-f3b93010247d@sha256:a64c74cbb713e640977bdf31979c7b46dc4aaaa9a8922676e4673ee2e1faf8cb`; worker image `worker-sha-f3b93010247d@sha256:52caec9f158ca768707a4b4705b3c9c9984c311d97f7673ad728dc018ca40e45`; GitOps rollout commit `0fc0f44 fix: deploy stripe sandbox payment gate`; GitOps CI run `27884849355` completed `success`; Argo `libreplay` is `Synced|Healthy|0fc0f44`.
- [x] Stripe remains blocked for production/live money. Evidence: source config rejects `DEPLOYMENT_MODE=production` with `PAYMENT_PROVIDER=stripe`, requires `STRIPE_SANDBOX_ONLY=true`, `STRIPE_SECRET_KEY=sk_test_*`, `STRIPE_WEBHOOK_SECRET=whsec_*` and `STRIPE_PSP_APPROVAL_REF`; GitOps manifests/CI contain no Stripe env or secrets and staging/production contracts remain `PAYMENT_PROVIDER=disabled`.
- [x] PPV checkout/webhook skeleton is sandbox-only and entitlement-safe. Evidence: PPV creates or reuses a pending local `STRIPE` transaction by user+PPV+amount+currency, passes the local transaction id as Stripe idempotency key/client reference, returns `PENDING` with redirect URL, and grants access only from a signed paid `checkout.session.completed` webhook.
- [x] Independent verifier re-audited the fix. Evidence: Heisenberg reported PASS after validating production rejection, staging sandbox gates, checkout idempotency, signed webhook handling, ADULT_PSP/mock compatibility, GitOps non-activation and compliance docs.
- [x] Local verification passed. Evidence: `pnpm --filter @libreplay/config test` passed `25`; `pnpm --filter @libreplay/media test` passed `25`; `pnpm --filter @libreplay/web test` passed `138`; focused Stripe/payment tests passed; `pnpm --filter @libreplay/config typecheck && pnpm --filter @libreplay/media typecheck && pnpm --filter @libreplay/web typecheck` passed; `git diff --check` passed.
- [x] Runtime validation passed after rollout. Evidence: LAN ConfigMap still has `DEPLOYMENT_MODE=lan-demo`, `PAYMENT_PROVIDER=mock`, `ENABLE_MOCK_PAYMENTS=true`; deployments contain no `STRIPE_*` env; `/api/health/deps` is `ok:true` with queues `0`; unsigned payment webhook remains `503 PAYMENT_PROVIDER_NOT_CONFIGURED`; focused LAN Playwright `e2e/33-payments-media.auth.spec.ts` returned `5 passed (8.7s)`; web/worker recent log greps found no `error|exception|failed|unhandled|stripe`.
- [blocked] Live PSP launch remains blocked. Evidence: no written Stripe live approval exists for LibrePlay's adult/dating/creator PPV/exclusive-content scope; Connect/KYC/KYB/payouts, refunds, disputes, chargebacks, tax/VAT, reconciliation and real safety-provider enforcement remain missing.

## 2026-06-20 PMO Iteration - Trusted Proxy Client IP Gate

- [x] Source client-IP policy is centralized and fail-closed for public deployments. Evidence: source commit `b7cd809 fix(security): trust client ip through explicit proxy policy` adds `apps/web/src/lib/client-ip.ts`, requires `TRUST_PROXY_CLIENT_IP=true` in staging/production, trusts only the configured header, returns `null` for untrusted staging/prod headers and makes rate limiting fail closed when a public deployment has no trusted client identity.
- [x] Route and env coverage prove the intended header behavior. Evidence: `client-ip.test.ts` covers Cloudflare header trust, explicit XFF mode, LAN fallback and staging trust-disabled null; `demo-login/route.test.ts` proves spoofed `x-forwarded-for` is ignored when `TRUSTED_CLIENT_IP_HEADER=cf-connecting-ip`; config/auth/web fixtures include the new staging trust flags.
- [x] Source validation passed locally and remotely. Evidence: local `pnpm test`, `pnpm typecheck`, `pnpm --filter @libreplay/web build` and `git diff --check` passed; source CI run `27862537072` completed `success`; Release Image run `27862689469` completed `success` for `sha-b7cd8097bb02`.
- [x] Official image digests were published. Evidence: web `sha256:24647560812ceed10e5643d14f90a0fbd084a86b8b73087c5f3271360bfdb18a`; worker `sha256:24c63701d3190485f8a165ec8dcc7d2f4168944ebcb740141dd98c38785c94f6`; tools `sha256:36c14a5a533161676d94c0d69b1063dc9cf7a48692608fd20eed0eb2df52bab2`; seed `sha256:d8ed5091bed3e3e7a220128ca3097702fdf98b507e9cdf997e4a4e6ed2538bab`.
- [x] GitOps deploy is complete for LAN and staging contract. Evidence: GitOps commit `5937cf8 fix: deploy libreplay trusted client ip policy`; GitOps CI run `27862856489` passed; Argo `libreplay` is `Synced|Healthy|5937cf81ad4ff7f6d54172df5d025eb304c07595`; runtime web/worker images match the `b7cd809` digests; live LAN config is `TRUST_PROXY_CLIENT_IP=false`, `TRUSTED_CLIENT_IP_HEADER=x-forwarded-for`, `RATE_LIMIT_BACKEND=redis`, `RATE_LIMIT_FAIL_MODE=closed`.
- [x] Staging contract and future runtime use an explicit Cloudflare trust boundary. Evidence: `staging/libreplay-staging-contract.yaml` and `staging/overlays/runtime/patch-configmap.yaml` set `TRUST_PROXY_CLIENT_IP=true` and `TRUSTED_CLIENT_IP_HEADER=cf-connecting-ip`; `staging/overlays/runtime/cloudflare-ipallowlist.middleware.yaml` allows the current official Cloudflare IPv4/IPv6 ranges; `scripts/check-libreplay-staging-runtime.sh` requires the middleware and rejects `x-forwarded-for` in staging.
- [x] Runtime regression passed after deploy. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true` and all queues `0`; `NODE_TLS_REJECT_UNAUTHORIZED=0 BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `93 passed (1.5m)`.
- [blocked] Public runtime client-IP proof remains blocked by staging secrets. Evidence: `scripts/check-libreplay-staging-preflight.sh` now confirms live staging config has `TRUST_PROXY_CLIENT_IP=true` and `TRUSTED_CLIENT_IP_HEADER=cf-connecting-ip`, but `ExternalSecret/libreplay-secrets` is still `Ready=False SecretSyncedError` and `Secret/libreplay-secrets` is not materialized, so staging runtime workloads remain intentionally unsynced.
- [blocked] LAN is not proof of public client-IP attribution. Evidence: a LAN login smoke with a custom `x-forwarded-for` still keyed Redis as `libreplay:rl:lan-demo:login:10.42.0.0`; the key was cleaned after capture. Public per-IP abuse budgets must be proven against the Cloudflare-backed staging runtime once real secrets exist.
- [blocked] Runtime log residual remains. Evidence: post-E2E log sweep found one `TypeError: Invalid state: ReadableStream is already closed`; current health recovered and E2E passed, but production readiness still needs runtime connection/stream observability and root-cause cleanup.

## 2026-06-20 PMO Iteration - Real Auth SMTP Staging Runtime Contract Gate

- [x] PMO/subagent audit separated existing static contract from missing live staging. Evidence: DevOps/Architecture, Security/Auth and Verifier passes confirmed `staging/` previously contained only Namespace/ExternalSecret/ConfigMap, `libreplay-staging` namespace/app did not exist, and live LAN config remains `DEPLOYMENT_MODE=lan-demo`, `USE_MOCK_OAUTH=true`, `AUTH_EMAIL_PROVIDER=console`, `PAYMENT_PROVIDER=mock`.
- [x] Staging runtime overlay exists and renders safely. Evidence: `staging/overlays/runtime` builds from `k8s` base, rewrites namespace to `libreplay-staging`, uses staging host `https://libreplay-staging.e-dani.com`, DB name `libreplay_staging`, Vault path `secret/libreplay/staging`, `DEPLOYMENT_MODE=staging`, `USE_MOCK_OAUTH=false`, `AUTH_EMAIL_PROVIDER=smtp`, `PAYMENT_PROVIDER=disabled`, `ENABLE_MOCK_PAYMENTS=false`, `ENABLE_LAN_DEMO_LOGIN=false`, required OAuth/SMTP secret refs and a public `traefik-edge` IngressRoute with external-dns annotations, Cloudflare-proxied target `57.129.17.172`, public TLSStore and no `ClientIP` or `sso-chain`.
- [x] Staging runtime anti-drift checker exists. Evidence: `scripts/check-libreplay-staging-runtime.sh` renders `staging/overlays/runtime`, runs client dry-run, requires all OAuth/SMTP/DB secret key names plus `traefik-edge`/external-dns/public TLS posture and rejects LAN host, LAN database, base Vault path, `dataFrom`, optional provider refs, mock OAuth/payments/email and LAN-only ingress.
- [x] Static contract validation remains green. Evidence: `scripts/check-libreplay-staging-contract.sh --static`, `kubectl apply --dry-run=client -k staging`, `kubectl apply --dry-run=client -k staging/overlays/runtime`, `kubectl kustomize k8s` plus client dry-run and `scripts/check-libreplay-staging-runtime.sh` all passed.
- [x] Source guardrails remain green. Evidence: `pnpm --filter @libreplay/config test -- src/__tests__/env.test.ts` returned `14 passed`; `pnpm --filter @libreplay/auth test -- src/__tests__/oauth-config.test.ts` returned `7 passed`; `PW_STAGING_REAL_AUTH=1 ... playwright test --project=staging-real-auth --list` lists 2 staging smokes.
- [x] Contract-only Argo app is prepared outside this repo for safe first live sync. Evidence: clean worktree `/home/dibanez/k8s/_worktrees/k8s-gitops-libreplay-staging` adds `apps/libreplay-staging.yaml` pointing to `repoURL=https://github.com/pocharlies/k8s-libreplay-pocharlies`, `targetRevision=deploy/prod`, `path=staging`, destination namespace `libreplay-staging`; root Kustomize render and client dry-run passed.
- [x] Contract-only staging is live. Evidence: `Application/libreplay-staging` targets `path=staging` and reports `Synced|Degraded|cb2c88101b90379310841742adf2cf3e39434626|staging`; namespace `libreplay-staging`, `ConfigMap/libreplay-config`, `ExternalSecret/libreplay-secrets`, `ExternalSecret/harbor-pull` and DNS-only `Service/libreplay-staging-dns-preflight` exist; no runtime Deployment/StatefulSet/IngressRoute/Pod resources are present.
- [x] Staging image pull secret contract is declared. Evidence: `ExternalSecret/harbor-pull` in `staging/libreplay-staging-contract.yaml` uses the already-proven Vault path `infra/harbor/ci-robot` and templates a `kubernetes.io/dockerconfigjson` Secret without storing registry credentials in Git.
- [x] Staging DNS preflight is live without runtime workloads. Evidence: `Service/libreplay-staging-dns-preflight` is an `ExternalName` with external-dns hostname `libreplay-staging.e-dani.com`, target `57.129.17.172` and Cloudflare proxy annotation; external-dns created `libreplay-staging.e-dani.com` and `a-libreplay-staging.e-dani.com`; preflight reports DNS resolution and TLS handshake OK.
- [x] Public edge namespace watch is ready for future runtime. Evidence: infra commit `56da136 feat(edge): watch libreplay staging namespace`; Argo `traefik-edge` is `Synced|Healthy|40.2.0,56da136993ea209d2173d999f876af39df16b67c`; DaemonSet rolled out and provider namespace args include `libreplay-staging` for both Kubernetes CRD and Ingress providers.
- [x] Staging preflight script exists. Evidence: `scripts/check-libreplay-staging-preflight.sh` checks static contract, runtime overlay, Argo path safety, absence of runtime workloads, DNS preflight service shape, live config posture, `libreplay-secrets`, `harbor-pull`, DNS and TLS without printing secret values; `--strict` turns blockers into a hard gate.
- [blocked] Live real-provider staging remains blocked by secrets only. Evidence: post-deploy preflight reports `ExternalSecret/libreplay-secrets` is `Ready=False SecretSyncedError` and `Secret/libreplay-secrets` is not materialized; `harbor-pull`, DNS and TLS are OK. Vault values for Google, Facebook, `OAUTH_TOKEN_ENC_KEY`, SMTP and internal app credentials have not been proven.
- [blocked] Staging runtime workloads must not be synced yet. Evidence: the renderable `staging/overlays/runtime` overlay defines web/worker/db/redis/minio/meili/ingress, but real OAuth/SMTP/app secrets are still absent, so only the contract path `staging` is safe for Argo.

## 2026-06-20 PMO Iteration - Visual Media Wash And Demo Asset Verification Gate

- [x] Visual asset subagent completed an independent audit before any new generation. Evidence: Godel reported `100` demo/prod-9a MediaAssets, `500` variants and `0` unlinked generated assets; UI consumption includes 78 Discover candidates with 70 prod9a avatars, 10 profile covers, 8 event covers, 6 date covers and 7 blog article cover links using 6 distinct blog covers.
- [x] No duplicate image generation was required for this pass. Evidence: runtime DB already had the requested 100 realistic generated demo images under `demo/prod-9a/%`; direct SQL at 06:30 CEST reports `assets=100`, `variants=500`, `avatars=70`, `profileCovers=10`, `eventCovers=8`, `dateCovers=6`, `blogCovers=6`.
- [x] Source visual wash patch is committed and pushed. Evidence: source commit `006088f feat(demo): validate and reuse visual media` adds `.lp-media-page`/`.lp-media-wash`, wires Discover/events/dates/blog to existing MediaAsset image URLs, and adds focused E2E `apps/web/e2e/34-demo-visuals.auth.spec.ts`.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/web typecheck`, `pnpm --filter @libreplay/web build`, `pnpm --filter @libreplay/web exec playwright test --project=member e2e/34-demo-visuals.auth.spec.ts --list` and `git diff --check` passed locally; source CI run `27859754713` passed after rerun and Release Image run `27859924402` passed for `sha-006088f2bdcc`.
- [x] Official image digests were published. Evidence: web `sha256:dc670d08fb8b990c9e48160129b08c122ee134d45fd63cf47aefa09aec723d49`; worker `sha256:69d2f71610bfe03b35eabb3c78e513892983a6163157489d5cabb85cbfb41d6b`; tools `sha256:964eb09052fe5efb059738ad38398c78fde342d3dbc038decebef44671fdb3b2`; seed `sha256:8c928f0d66df9c30490489c3ca033ec8ab92652964221f3461bba69c29351257`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `76cdd55 fix: deploy libreplay visual media wash`; GitOps CI run `27860101187` passed; client and server dry-runs of `k8s/manifest.yaml` passed; Argo is `Synced/Healthy` at `76cdd552be06b46e313393cd1981b3448fe8a8c9`.
- [x] Runtime is on expected images and healthy. Evidence: `libreplay-web` is `1/1` on `sha-006088f2bdcc@sha256:dc670d08fb8b990c9e48160129b08c122ee134d45fd63cf47aefa09aec723d49`; `libreplay-worker` is `1/1` on `worker-sha-006088f2bdcc@sha256:69d2f71610bfe03b35eabb3c78e513892983a6163157489d5cabb85cbfb41d6b`; `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true` and all queues `0`.
- [x] Runtime visual smoke passed after deploy. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --project=member e2e/34-demo-visuals.auth.spec.ts --reporter=line` returned `3 passed (7.6s)`, covering `/api/media` variants, image-backed Discover/events/dates/blog pages and real map tiles via `/api/map/tiles`.
- [x] Recent runtime logs are clean for this rollout. Evidence: 10-minute web and worker log greps for `error|exception|failed|p1001|readablestream` returned no matches.
- [blocked] Direct Google Maps integration remains blocked. Evidence: no validated Google Maps API key, billing/project policy or provider terms are configured; the production-safe LAN behavior remains the same-origin OpenStreetMap tile proxy.
- [blocked] Full production visual/media readiness remains incomplete. Evidence: demo images are not a production moderation policy; production still needs contracted map/tile provider policy, CDN/signed media delivery, lifecycle/retention, real CSAM/media moderation, legal/privacy review and observability before real users.

## 2026-06-20 PMO Iteration - Distributed Rate Limiting Abuse Gate

- [x] Source Redis rate-limit patch committed and pushed. Evidence: source commit `4be4785 feat(security): add redis abuse rate limiting`.
- [x] Central limiter uses shared Redis in deployed environments. Evidence: `apps/web/src/lib/rate.ts` selects Redis when `RATE_LIMIT_BACKEND=redis`, keys by `RATE_LIMIT_REDIS_PREFIX:DEPLOYMENT_MODE:name:id`, uses a fixed-window Lua script with `RATE_LIMIT_TIMEOUT_MS`, and production/staging config requires Redis plus fail-closed policy.
- [x] Explicit route budgets cover high-churn mutations. Evidence: auth/OAuth callback, swipes, posts/comments/reactions/bookmarks/delete, conversations, reports/messages/uploads/media complete, map live/checkin, AI/LLM, payments, creator content and events routes call named rate-limit buckets.
- [x] Source validation passed. Evidence: focused tests returned `@libreplay/security rate-limit 1 passed`, `@libreplay/web rate.test 4 passed`, `security-idor.test.ts 17 passed`, `@libreplay/config env.test 14 passed`; full gates `pnpm --filter @libreplay/web test` -> `45 passed`, `pnpm typecheck`, `pnpm --filter @libreplay/web build`, `pnpm test`, `pnpm lint`, `pnpm audit --prod` and `git diff --check` all passed.
- [x] Source CI and release image workflow passed. Evidence: source CI run `27854730178` completed `success`; Release Image run `27854887817` completed `success` with `version=sha-4be4785bf83b`.
- [x] Official image digests were published. Evidence: web `sha256:c80a9bd7c319792767e5f01ab5f169c2cbb78ed7c02e07d3878554f5fed7f0af`; worker `sha256:09f5127a565f3823b4c671ebc19ca451a681cfde0c16dbdeb911cbcf9199fc0e`; tools `sha256:b8f39e6446ce929aa5d1287b0afed08717b78135dd40671394d73a94eb5927f5`; seed `sha256:c45d22ab4b1a6919e01a227ec0c43b5316ba5356888fdbfef005ce3bf4b47564`.
- [x] GitOps deploy is complete and current. Evidence: GitOps commit `9215e80 fix: deploy libreplay redis rate limits`; GitOps CI run `27855093322` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `9215e804d91e08f0e064f53773064107a628e7b2`.
- [x] Runtime is on expected images and Redis rate-limit config. Evidence: `libreplay-web` is `1/1` on `sha-4be4785bf83b@sha256:c80a9bd7c319792767e5f01ab5f169c2cbb78ed7c02e07d3878554f5fed7f0af`; `libreplay-worker` is `1/1` on `worker-sha-4be4785bf83b@sha256:09f5127a565f3823b4c671ebc19ca451a681cfde0c16dbdeb911cbcf9199fc0e`; env has `DEPLOYMENT_MODE=lan-demo`, `RATE_LIMIT_DISABLED=false`, `RATE_LIMIT_BACKEND=redis`, `RATE_LIMIT_FAIL_MODE=closed`, `RATE_LIMIT_TIMEOUT_MS=250`.
- [x] Runtime abuse smoke proves Redis enforcement. Evidence: seven invalid same-origin login POSTs returned first five `400 VALIDATION_ERROR` then two `429 RATE_LIMITED`; Redis key `libreplay:rl:lan-demo:login:10.42.0.0` reached `ttl=870 value=7` and was deleted after evidence capture.
- [x] Runtime health, queues, logs and full LAN regression are clean. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true` and all queues `0`; web/worker log grep for `error|exception|failed` returned no matches; `POST /api/payments/webhook` remains `503 PAYMENT_PROVIDER_NOT_CONFIGURED`; cross-site `POST /api/events` returns `403 INVALID_ORIGIN`; full LAN Playwright returned `90 passed (1.5m)`.
- [blocked] Full production abuse/security remains blocked. Evidence: the trusted proxy/client-IP source and staging contract gate exists, but public runtime proof is still blocked by missing staging secrets/workloads; LAN smoke still keys login by Traefik/cluster IP `10.42.0.0`; real webhook signatures, real providers/secrets, secrets rotation and compliance controls remain open.

## 2026-06-20 PMO Iteration - Demo Visual Content, Real Map And Blog/Event Imagery Gate

- [x] Image subagents generated about 100 safe realistic demo images. Evidence: asset-generation lanes Carson and Boyle produced 70 profile avatars plus 30 covers; committed manifest `packages/jobs/src/seed/demo-visuals.manifest.json` contains `100` URL-only assets with counts `70 profile_avatar`, `10 profile_cover`, `8 event_cover`, `6 date_cover`, `6 blog_cover`; model evidence records requested `image-gpt-2.0`, exact selectable model absent, generated provider/model `gpt-image-2`; sampled images were inspected before commit.
- [x] Demo profile images are integrated through the media pipeline. Evidence: source commit `18aeffb feat(demo): add visual seed and real map` adds idempotent media seeding through S3-compatible storage and variant generation; runtime SQL after deploy reports `media_assets=100`, `media_variants=500`, `demo_visual_users=70`, `profile_avatars=70`, `profile_covers=10`.
- [x] Events, dates and blog receive visual covers. Evidence: source commit `eed2ff4 fix(seed): spread demo blog covers`; runtime SQL reports `event_covers=8`, `date_covers=6`, `blog_covers=7`, `demo_blog_covers=6`; seed log for `libreplay-db-seed-eed2ff4` ended `demo visuals seed: done 100/100`.
- [x] Visual surfaces use first-party media URLs and variants. Evidence: Discover/profile/events/dates/blog code paths render `/api/media/[id]?variant=...`; media permission tests cover approved public event covers and reject private date covers; runtime E2E covered Discover, profile, events, dates and blog.
- [x] Map uses a real-tile fallback with same-origin proxy. Evidence: `/es/map` renders tile images with `src*="/api/map/tiles/"`; API route proxies OSM server-side with auth/cache; page CSP `img-src` contains `'self' data: blob: http://localhost:9000` and does not expose OSM domains to the browser.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/db generate`, Prisma validate, `pnpm typecheck`, `pnpm test`, `pnpm lint`, `pnpm audit --prod`, `pnpm --filter @libreplay/web build`, focused web media/map/IDOR tests and media permission tests all passed for `18aeffb`; follow-up `eed2ff4` ran `pnpm --filter @libreplay/jobs typecheck` and `pnpm --filter @libreplay/jobs test` -> `8 passed`.
- [x] Source CI and release image workflows passed. Evidence: source CI `27857859914` and Release Image `27858038698` passed for `18aeffb`; source CI `27858493902` and Release Image `27858645816` passed for `eed2ff4`. Final digests: web `sha256:ff7e1e94017b041a145d542bb542299bb6cae04a9e554b7d61cf49c334a79ba2`, worker `sha256:7d1de009f5f4b1add25076e65c8297b02335513aab458af276f2aae82b41bbbc`, seed `sha256:3de9e8c7afa69701b6a23ab81a8d93dce1a81543178d70e63ba955e751e84812`.
- [x] GitOps deploy completed. Evidence: GitOps commit `2efac7f feat: deploy libreplay demo visuals` and CI `27858269590` passed; follow-up GitOps commit `177414d fix: deploy libreplay blog visual seed` and CI `27858810665` passed; server-side dry-run passed; Argo is `Synced/Healthy` at `177414da054b376ab76358a8f8f1b93e367b344b`.
- [x] Runtime is on expected images and healthy after deploy. Evidence: web `1/1` on `sha-eed2ff4d2b18@sha256:ff7e1e94017b041a145d542bb542299bb6cae04a9e554b7d61cf49c334a79ba2`; worker `1/1` on `worker-sha-eed2ff4d2b18@sha256:7d1de009f5f4b1add25076e65c8297b02335513aab458af276f2aae82b41bbbc`; seed job `libreplay-db-seed-eed2ff4` completed `1/1` in `6m7s`; `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true`, queue counts `0`; final post-E2E 2-minute web log grep returned no new matches.
- [x] Mobile and desktop validation passed. Evidence: `NODE_TLS_REJECT_UNAUTHORIZED=0 BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.7m)`, including mobile map flow `map mobile flow can request nearby profiles without covering controls`.
- [x] PMO re-audit completed. Evidence: independent auditor Avicenna flagged media-cover IDOR, manifest selection, undeployed GitOps, CSP wiring, browser OSM leakage and mobile-control overlap; follow-up code/deploy changed media permissions, manifest selection, GitOps rollout, CSP in Next config, same-origin tile proxy and mobile map control spacing.
- [blocked] Production visual/media readiness remains incomplete. Evidence: generated demo images are LAN demo data, not production moderation policy; production still needs a contracted map/tile provider policy, CDN/signed media delivery, media lifecycle/retention, real CSAM/media moderation and legal/privacy review. A 15-minute post-run log window also showed transient Prisma `P1001` database reachability errors and one `ReadableStream is already closed` during the E2E window; current health recovered, but production needs connection/readiness observability before sign-off.

## 2026-06-20 PMO Iteration - Remaining BOLA Visibility Ownership Gate

- [x] Source visibility/ownership patch committed and pushed. Evidence: source commit `7a7d566 fix(security): close remaining visibility idor`.
- [x] Events public surfaces are constrained to published public events. Evidence: `canViewEvent` requires `status === PUBLISHED` and `visibility === PUBLIC_VERIFIED`; API detail/RSVP/review/report/purchase mock routes call it before side effects; SSR event detail page returns `notFound()` before render when the helper rejects.
- [x] Event listings no longer advertise private/non-public events. Evidence: API events list, events page, landing event count, club page embedded events and club API embedded events all filter `status: PUBLISHED` plus `visibility: PUBLIC_VERIFIED`.
- [x] Groups public surfaces are constrained to public verified groups. Evidence: `canViewGroup` requires `PUBLIC_VERIFIED`; group API detail/join/leave and SSR group detail page call it before membership side effects or render.
- [x] Creator paid-content `postId` ownership is enforced. Evidence: `/api/creator/paid-content` verifies referenced post exists, belongs to `session.user.id` and is not deleted before `paidContent.create` or `recordActivity`; tests cover foreign post and deleted own post.
- [x] Blog club attribution is enforced. Evidence: `/api/blog/submit` verifies `clubProfileId` belongs to the submitting user or an admin before slug lookup and `blogArticle.create`.
- [x] Focused no-side-effect tests exist. Evidence: `apps/web/src/app/api/security-idor.test.ts` now has 11 tests covering events, groups, paid-content and blog negative paths, with assertions that `eventAttendee`, `eventReview`, `purchase`, `groupMember`, `paidContent`, `recordActivity`, `blogArticle`, `report` and moderation side effects are not called.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/web test -- src/app/api/security-idor.test.ts` -> 11 passed; `pnpm --filter @libreplay/web typecheck`; `pnpm --filter @libreplay/web test` -> 35 passed; `pnpm --filter @libreplay/web build`; `pnpm typecheck`; `pnpm test`; `pnpm lint`; `pnpm audit --prod`; and `git diff --check` all passed.
- [x] Independent PMO verification passed. Evidence: McClintock and Raman identified route semantics; Boole initially failed the SSR detail leak and missing deleted-post test, then re-verified PASS after the fixes.
- [x] Source CI and release image workflow passed. Evidence: source CI run `27853674582` completed `success` in 6m04s; Release Image run `27853851439` completed `success` in 5m41s with `version=sha-7a7d56663dfb`.
- [x] Official image digests were published. Evidence: web `sha256:76dd4dda6d29eda1654a46dac3caa406438b83ca110530a70fb5afa8549acd28`; worker `sha256:abcc320b67c4958c3d375fb4f2aa0a6ec28d77f97d2ba79f258362b4b2f22103`; tools `sha256:3a951a6b76e886c5eface4e2a9de3c230374b17ea88dee7166d149e42cad0553`; seed `sha256:b906b7bcb99c16bef11a990a7e98dfb74d2da2a7759af1a437a91a54358b1ba9`.
- [x] GitOps deploy is complete and current. Evidence: GitOps commit `458bb88 fix: deploy libreplay visibility idor guards`; GitOps CI run `27854062401` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `458bb8857553b22b17b38483c409981e9c69534e`.
- [x] Runtime is on expected images. Evidence: `libreplay-web` is `1/1` on `sha-7a7d56663dfb@sha256:76dd4dda6d29eda1654a46dac3caa406438b83ca110530a70fb5afa8549acd28`; `libreplay-worker` is `1/1` on `worker-sha-7a7d56663dfb@sha256:abcc320b67c4958c3d375fb4f2aa0a6ec28d77f97d2ba79f258362b4b2f22103`; rollout annotation is `visibility-idor-gate-20260620-0205`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true`; authEmail and mediaModeration queues waiting/active/delayed/failed all `0`; recent web and worker log greps for `error|exception|failed` returned no matches.
- [x] Runtime mutation smoke still passes. Evidence: cross-site `POST /api/events` with `Origin: https://evil.example` and `Sec-Fetch-Site: cross-site` returned `403 INVALID_ORIGIN`.
- [x] Full LAN regression validation passed after deploy. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.5m)`.
- [blocked] Full production security remains blocked. Evidence: browser double-submit CSRF and Redis/distributed rate limiting are now deployed, but real webhook signatures, real providers/secrets, secrets rotation and compliance controls remain open.

## 2026-06-20 PMO Iteration - Site-Wide CSRF/IDOR Mutation Gate

- [x] Source security gate patch committed and pushed. Evidence: source commit `c3ca34b fix(security): gate api mutations and visibility`.
- [x] Current mutation inventory exists. Evidence: `docs/security-mutation-inventory.md` records 82 mutating route files and 86 `POST|PUT|PATCH|DELETE` handlers; before this meta only 10 mutating route files had local `enforceAuthOrigin` calls.
- [x] Site-wide browser-origin gate is deployed. Evidence: `apps/web/src/middleware.ts` now matches `/api/:path*`, checks `POST|PUT|PATCH|DELETE` mutations with Origin/Referer/Fetch Metadata, rejects `Sec-Fetch-Site: cross-site`, and exempts only `/api/payments/webhook`.
- [x] LAN compatibility is preserved. Evidence: source middleware tests cover LAN/demo API clients that omit Origin; runtime same-origin unauthenticated POST to `/api/posts` returned `307` instead of `INVALID_ORIGIN`, so auth flow remains responsible after same-origin acceptance.
- [x] Payment webhook exemption is not an open mutation. Evidence: runtime `POST /api/payments/webhook` returned `503 PAYMENT_PROVIDER_NOT_CONFIGURED`; real PSP webhook signature verification remains blocked until provider integration.
- [x] P0 date proposal visibility IDOR fixed. Evidence: `canViewDateProposal` guards `/api/dates/[id]` and `/api/dates/[id]/apply`; unit tests reject private date detail/apply by another user before `DateApplication` upsert.
- [x] P0 post visibility IDOR fixed. Evidence: `canViewPost` guards post detail/react/comments/bookmark/report; unit tests reject private post interactions by another user before reaction/comment/bookmark/report/moderation side effects.
- [x] Additional high-risk negative tests exist. Evidence: `apps/web/src/app/api/security-idor.test.ts` covers media owner mismatch, conversation non-member message, paid-content owner mismatch, non-moderator moderation action, date visibility and post visibility.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/web test -- src/middleware.test.ts`; `pnpm --filter @libreplay/web test -- src/app/api/security-idor.test.ts`; `pnpm --filter @libreplay/web test`; `pnpm --filter @libreplay/web typecheck`; `pnpm typecheck`; `pnpm test`; `pnpm --filter @libreplay/web build`; `pnpm lint`; `pnpm audit --prod`; and `git diff --check` all passed.
- [x] Independent security verifier passed. Evidence: Fermat reported PASS for commit/deploy and no residual P0 blockers after inspecting middleware, exact webhook exemption, date/post guards and focused tests.
- [x] Source CI and release image workflow passed. Evidence: source CI run `27852395400` completed `success`; Release Image run `27852581835` completed `success` with `version=sha-c3ca34b73c57`.
- [x] Official image digests were published. Evidence: web `sha256:fa19e023ba2d11ea3cd4fc0572b5661f61b531ed8656360eccf07425b898cd13`; worker `sha256:b23bd33f5a862e006f3ed9fa5c66672272c922870d605b0f3bb70165e6c32378`; tools `sha256:0cab6cff2e64965730ddfc9c70d192d0e881594d2421160e358f9d71bc7c4db5`; seed `sha256:e8f463fd4cfe1ffcf10e9df2c74e8fb5897a07da377761eb24bbfdd72ee8735c`.
- [x] GitOps deploy is complete and current. Evidence: GitOps commit `1bc61b9 fix: deploy libreplay security mutation gate`; GitOps CI run `27852814357` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `1bc61b960643ce2fb8d37c3be2626e80ab857329`.
- [x] Runtime is on expected images. Evidence: `libreplay-web` is `1/1` on `sha-c3ca34b73c57@sha256:fa19e023ba2d11ea3cd4fc0572b5661f61b531ed8656360eccf07425b898cd13`; `libreplay-worker` is `1/1` on `worker-sha-c3ca34b73c57@sha256:b23bd33f5a862e006f3ed9fa5c66672272c922870d605b0f3bb70165e6c32378`; rollout annotation is `security-mutation-gate-20260620-0122`.
- [x] Runtime CSRF smoke passed. Evidence: cross-site `POST /api/posts` with `Origin: https://evil.example` and `Sec-Fetch-Site: cross-site` returned `403 INVALID_ORIGIN`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed all `0`; recent web and worker log greps for `error|exception|failed` returned no matches.
- [x] Full LAN regression validation passed after deploy. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.5m)`.
- [x] Follow-up BOLA/IDOR meta has closed the named remaining gaps. Evidence: `2026-06-20 PMO Iteration - Remaining BOLA Visibility Ownership Gate` covers events/groups/paid-content/blog with source, CI, release, GitOps, runtime and E2E evidence.

## 2026-06-20 PMO Iteration - Payment Fail-Closed And PSP Abstraction

- [x] Source payment fail-closed patch committed and pushed. Evidence: source commit `d2438a0 fix(payments): fail closed mock provider`.
- [x] Payment provider selection is environment-driven. Evidence: `PAYMENT_PROVIDER` accepts `disabled|mock`, defaults to `disabled`, and source config rejects `PAYMENT_PROVIDER=mock` unless `ENABLE_MOCK_PAYMENTS=true` and the runtime is test/development/LAN demo.
- [x] Staging/production cannot boot with demo monetization enabled. Evidence: source config rejects staging/production when `ENABLE_MOCK_PAYMENTS=true` or `PAYMENT_PROVIDER=mock`; `staging/libreplay-staging-contract.yaml` now declares `PAYMENT_PROVIDER=disabled` and `ENABLE_MOCK_PAYMENTS=false`.
- [x] Mock purchase routes fail closed before session or DB mutation when the provider is disabled. Evidence: six `/mock` purchase routes call `requireMockPaymentsRoute()` before session/Prisma work; `apps/web/src/app/api/payments/mock-routes.test.ts` verifies all six return `503 PAYMENT_PROVIDER_NOT_CONFIGURED` in staging-disabled config.
- [x] PPV purchases no longer bypass the payment provider. Evidence: `apps/web/src/app/api/creator/ppv/[id]/purchase/mock/route.ts` calls `chargePayment()` and stores the returned provider/reference instead of creating a `COMPLETED` purchase directly.
- [x] Provider-unavailable payment paths map to an explicit 503. Evidence: `apps/web/src/lib/payments.ts` wraps provider errors as `PAYMENT_PROVIDER_NOT_CONFIGURED`; `apps/web/src/app/api/payments/webhook/route.ts` returns the same code until a real PSP webhook is implemented.
- [x] Source validation passed. Evidence: focused config/media/web payment tests passed; `pnpm typecheck`; `pnpm test`; `pnpm --filter @libreplay/web build`; `pnpm audit --prod`; `pnpm lint`; and `git diff --check` all passed.
- [x] Source CI and release image workflow passed. Evidence: source CI run `27851079378` completed `success`; first Release Image run `27851277222` failed as expected because the version input was omitted; corrected Release Image run `27851301076` completed `success` with `version=sha-d2438a0ff2cb`.
- [x] Official image digests were published. Evidence: web `sha256:f11312ad087fe409ca14e26fc7029f25e177c951db18d8d619cb9047f5a83f13`; worker `sha256:aa0284ebe199dde396c0a13da03268b32aaa127341b29b0a62000888696e5730`; tools `sha256:e3441c79e587e6195ce0d0511ef95b780cbee1613484a57c7b75b8cb209ee02f`; seed `sha256:283ad14a2de2c162cde7cef29d2c51377d347f3ca5e02fe71f954ae5d49e62f1`.
- [x] GitOps deploy is complete and current. Evidence: GitOps commit `a8dbe19 fix: deploy libreplay payment fail closed`; GitOps CI run `27851552472` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `a8dbe190e6396f03efc3bcd00f8c6dff25dbaa8d`.
- [x] Runtime is on expected images and LAN-only payment posture. Evidence: `libreplay-web` is `1/1` on `sha-d2438a0ff2cb@sha256:f11312ad087fe409ca14e26fc7029f25e177c951db18d8d619cb9047f5a83f13`; `libreplay-worker` is `1/1` on `worker-sha-d2438a0ff2cb@sha256:aa0284ebe199dde396c0a13da03268b32aaa127341b29b0a62000888696e5730`; web pod env has `DEPLOYMENT_MODE=lan-demo`, `PAYMENT_PROVIDER=mock`, and `ENABLE_MOCK_PAYMENTS=true`.
- [x] Runtime webhook fail-closed check passed. Evidence: `POST https://libreplay.lan.e-dani.com/api/payments/webhook` returned `503` with `PAYMENT_PROVIDER_NOT_CONFIGURED`.
- [x] Runtime payment/media LAN validation passed. Evidence: focused LAN media/payment E2E returned `5 passed (7.5s)`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed all `0`; recent web and worker log greps for `error|exception|failed` returned no matches.
- [x] Full LAN regression validation passed after deploy. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.4m)`.
- [x] Specialist PMO passes completed. Evidence: Hegel identified the provider/config contract gap and direct PPV bypass; Noether required fail-closed mock route coverage, staging `PAYMENT_PROVIDER=disabled`, webhook 503, and preserving Stripe as an approval-blocked PSP decision.
- [blocked] Real production payments remain blocked. Evidence: no adult-friendly PSP has been selected/approved; webhook idempotency/signature verification, refunds, disputes, subscriptions, ledger, tax/VAT, creator payout/KYC and chargeback controls are not implemented.

## 2026-06-20 PMO Iteration - Video Renditions And HLS

- [x] Source video rendition patch committed and pushed. Evidence: source commit `2a04229 feat(media): add video renditions`.
- [x] Schema migration exists for video variants. Evidence: migration `20260619212500_video_renditions` adds `VIDEO_MP4_480P` and `VIDEO_HLS_480P` to `MediaVariantKind`.
- [x] Worker now produces compressed MP4 and HLS artifacts. Evidence: `packages/media/src/video.ts` writes deterministic `video_480p.mp4`, `hls_480p/index.m3u8` and `hls_480p/segment_###.ts` keys; ffmpeg uses bounded 480p libx264 CRF 28 output plus HLS VOD segments.
- [x] Variant access is authorized and range-aware. Evidence: `apps/web/src/app/api/media/[id]/variants/[variantId]/[[...path]]/route.ts` checks session and `canViewMediaAsset`, verifies variant ownership, allows only HLS `index.m3u8` or `segment_###.ts`, supports valid MP4 byte ranges, and maps invalid ranges to `416`.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/db generate`; `pnpm --filter @libreplay/media test -- video.test.ts`; `pnpm --filter @libreplay/jobs test -- media-moderation.test.ts`; `pnpm --filter @libreplay/web test -- 'src/app/api/media/[id]/variants/[variantId]/[[...path]]/route.test.ts'`; `pnpm typecheck`; `pnpm test`; `pnpm --filter @libreplay/web build`; Prisma validate; `pnpm lint`; `pnpm audit --prod`; `git diff --check` all passed.
- [x] Independent verifier blocked a weak range parser and the issue was fixed. Evidence: Nash rejected inverted ranges before source close; `parseSingleByteRange` now rejects malformed/inverted/multirange requests and source route tests pass.
- [x] Source CI and release image workflow passed. Evidence: source CI run `27849729670` completed `success`; Release Image run `27849982311` completed `success` for source `2a042298f46f5a696cd4018eded3662c4130cf31`.
- [x] Official image digests were published. Evidence: web `sha256:db7e0ed3db32a2fc8b865698b47318be4315c0c36c8e9042e8e0355c2145f149`; worker `sha256:26e07b1b51b5781753dcfffef675bfba95ca09613402b649bfb78dc2184eb077`; tools `sha256:a72e09f75b44f710692f999c8607dbaab013450ea5a071420738d69dd22bd41c`; seed `sha256:89fd1c3b54bbd0fa0a7604bc7d6aa52af258db6433a2dce6e44b2acdc2e4296d`.
- [x] GitOps deploy is complete and current. Evidence: GitOps commit `6437736 feat: deploy libreplay video renditions`; follow-up commit `32ad2dc fix: keep libreplay migration job history`; GitOps CI runs `27850274935` and `27850375820` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `32ad2dc30f9a99d72aaadeca6c2b3e70be85d6f7`.
- [x] Historical migration job state is preserved for Argo with prune disabled. Evidence: old completed job `libreplay-db-migrate-c37cdca` and new completed job `libreplay-db-migrate-2a04229` are both present in desired state; this resolved the prior extraneous-resource OutOfSync without manual prune.
- [x] Runtime is on expected images and worker memory was raised for transcoding. Evidence: `libreplay-web` is `1/1` on `sha-2a042298f46f@sha256:db7e0ed3db32a2fc8b865698b47318be4315c0c36c8e9042e8e0355c2145f149`; `libreplay-worker` is `1/1` on `worker-sha-2a042298f46f@sha256:26e07b1b51b5781753dcfffef675bfba95ca09613402b649bfb78dc2184eb077`; web and worker memory limits are `1Gi`.
- [x] Runtime video variant validation passed. Evidence: focused LAN media E2E `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test e2e/33-payments-media.auth.spec.ts --project=member --reporter=line` returned `5 passed (7.2s)`.
- [x] Runtime DB contains generated video variants. Evidence: latest video asset `cmqlh6b4m000d4ozyuwcxz4ry` has `THUMB_LARGE|image/jpeg|222`, `VIDEO_MP4_480P|video/mp4|2184`, and `VIDEO_HLS_480P|application/vnd.apple.mpegurl|6162`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps` reports postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed all `0`; recent web logs had no error matches; worker logs show media jobs completed with no processing errors.
- [x] Full LAN regression validation passed after deploy. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.5m)`.
- [blocked] Full production media readiness remains blocked. Evidence: the MVP produces one 480p MP4 plus HLS variant, but production still lacks a bitrate ladder, CDN/signed delivery strategy, async progress UI, lifecycle/retention policy, replay/dead-letter runbook, real CSAM/media moderation providers and multi-replica worker scaling.

## 2026-06-19 PMO Iteration - Authenticated Mobile Core And Discover Layout

- [x] Source mobile coverage patch committed and pushed. Evidence: source commit `0b712f2 test(web): tighten mobile message assertion` after initial coverage commit `7a03ffc`; Playwright mobile list reports `11 tests in 2 files`, including 6 authenticated mobile core tests in `16-mobile-auth.mobile.spec.ts`.
- [x] Independent QA rejected weak assertion and it was fixed. Evidence: Carson reported FAIL because the mobile message test only checked any `.lp-bubble.me`; source commit `0b712f2` now asserts `.lp-bubble.me` with the unique message text.
- [x] Runtime mobile E2E found a real responsive bug. Evidence: first LAN mobile run on `0b712f2` returned `9 passed, 2 failed`; Discover filters kept a desktop `280px minmax(0,1fr)` grid on iPhone and pushed the `Mensaje` control outside the viewport.
- [x] Discover mobile layout bug fixed. Evidence: source commit `8d64302 fix(web): stack discover filters on mobile`; `DiscoverClient` uses `lp-discover-main with-filters`; CSS stacks filters to one column below `900px` while preserving desktop two-column layout and sticky filters.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/web typecheck`; `pnpm --filter @libreplay/web test`; `pnpm --filter @libreplay/web build`; `pnpm --filter @libreplay/web exec playwright test --list --project=mobile`; `git diff --check`; source CI run `27848224011` completed `success`.
- [x] Official release digests were published. Evidence: Release Image run `27848471274` completed `success`; web digest `sha256:2039dfb76f3aeb90517c76a540ad2e4de982632af4339aa8055db6621979f7e4`; worker digest `sha256:d10c8b95909fbe2a45108ad4c9e16fe69a70e0c1d2c89a70b8ecaf2b382e497e`; tools digest `sha256:a2ceced4c83ce63506f00dfa1d27d0cd5bb2976714923a8d75c231ea55fa0ccd`; seed digest `sha256:ca07e18e917625e73d5fa7002bb55c75f6a07e9cf56a53894ea1427263f752a9`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `aa85ca3 fix: deploy libreplay mobile discover layout`; GitOps CI run `27848721618` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `aa85ca3a51a438a3e0a24a89886d9c47bdbebb90`.
- [x] Runtime is on expected images. Evidence: `libreplay-web` is `1/1` on `sha-8d643024885d@sha256:2039dfb76f3aeb90517c76a540ad2e4de982632af4339aa8055db6621979f7e4`; `libreplay-worker` is `1/1` on `worker-sha-8d643024885d@sha256:d10c8b95909fbe2a45108ad4c9e16fe69a70e0c1d2c89a70b8ecaf2b382e497e`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps=200`; postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed are all `0`; recent web/worker log grep for `error|exception|failed|token=|actionUrl|reset-password|verify-email` returned no matches.
- [x] Runtime mobile validation passed. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --project=mobile --reporter=line` returned `11 passed (10.0s)`.
- [x] Runtime desktop regression validation passed. Evidence: focused member desktop run for Discover, map and settings returned `8 passed (11.1s)`.
- [x] Full LAN E2E validation passed. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 pnpm --filter @libreplay/web exec playwright test --reporter=line` returned `90 passed (1.4m)`.
- [x] Specialist PMO passes completed. Evidence: Carson initially blocked certification due weak message assertion; Sartre reported PASS for the Discover responsive fix and confirmed the `+0.5` tap-target tolerance does not hide horizontal offscreen bugs.
- [blocked] Full mobile product readiness remains blocked. Evidence: authenticated mobile core flows and the later PWA installability/safe-area gate are covered, but mobile upload/camera capture, verification UX, creator checkout/admin moderation mobile paths, Android/iOS/browser matrix, offline/cache policy, mobile performance evidence and automated accessibility checks remain open.
- [blocked] Production readiness remains blocked. Evidence: LAN still uses mock OAuth, console email and mock payments; real Google/Facebook/OAuth encryption/SMTP/payment/provider/compliance/observability gates remain open.

## 2026-06-19 PMO Iteration - Pending Social Email And Mobile Auth Coverage

- [x] Source pending-social patch committed and pushed. Evidence: source commit `783bb8d feat(auth): complete pending social email flow`.
- [x] OAuth `pending_email` no longer points to a missing page. Evidence: source adds `/[locale]/auth/social-email` and `POST /api/auth/social-email`; Next build lists both `/[locale]/auth/social-email` and `/api/auth/social-email`.
- [x] Pending social profile cookie is sealed and token-free. Evidence: callback stores a profile with `accessToken=null`, `refreshToken=null`, `raw={}` through `sealPendingOAuthProfile`; `openPendingOAuthProfile` rejects payloads that contain email, provider tokens or raw provider data and enforces a 10-minute TTL.
- [x] Manual social-email completion is conflict-safe. Evidence: `completePendingOAuthEmail` normalizes `trim().toLowerCase()` emails, forces `emailVerified=false`, creates `User` + `AuthAccount` in a Prisma transaction, catches P2002 provider/email races as `email_conflict`, and never stores pending provider tokens.
- [x] Social-email route has security controls. Evidence: route enforces `enforceAuthOrigin`, IP rate limiting, sealed pending-cookie precondition, `ALREADY_AUTHENTICATED` rejection, `safeInternalRedirect`, pending-cookie deletion, session creation and queued email verification.
- [x] OAuth negative and mobile auth coverage expanded. Evidence: default Playwright list now reports `84 tests in 22 files`; new checks cover OAuth callback unknown/missing/malformed/no-cookie state, link-intent login redirect, social-email missing-cookie rejection, mobile OAuth buttons and mobile forgot/reset/verify/social-email routes.
- [x] Source validation passed. Evidence: `pnpm test`; `pnpm typecheck`; `pnpm --filter @libreplay/web build`; `git diff --check`; source CI run `27846232443` completed `success`.
- [x] Official release digests were published. Evidence: Release Image run `27846479315` completed `success`; web digest `sha256:95e6e4614879e40ff0d03552453a5c66f3ad7abba345b96afe7fdc1a6463ed55`; worker digest `sha256:ffdd4cf9c6679ab69f7d3dae9d7103038e0a0f0c7cc626bea52fdd5dd24da979`; tools digest `sha256:4570443a2be3c41364e173954366c755051c815136f924a89b421a83d23f1da6`; seed digest `sha256:63ffcd43832aef5845328425e0a4d015db73173c0197ddf6bed9e85ec905bc29`.
- [x] GitOps deploy is complete. Evidence: GitOps commit `40c2faa feat: deploy libreplay pending social auth`; GitOps CI run `27846770418` completed `success`; Argo is `Synced/Healthy` at `40c2faaec74aaf01e84c7cfdf63a84098e9c0b37`.
- [x] Runtime is on expected images. Evidence: `libreplay-web` is `1/1` on `sha-783bb8da7338@sha256:95e6e4614879e40ff0d03552453a5c66f3ad7abba345b96afe7fdc1a6463ed55`; `libreplay-worker` is `1/1` on `worker-sha-783bb8da7338@sha256:ffdd4cf9c6679ab69f7d3dae9d7103038e0a0f0c7cc626bea52fdd5dd24da979`.
- [x] Runtime health, queues and logs are clean. Evidence: `/api/health/deps=200`; postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed are all `0`; recent web/worker log grep for `error|exception|failed|token=|actionUrl|reset-password|verify-email` returned no matches.
- [x] Runtime Playwright validation passed. Evidence: focused auth/mobile run returned `19 passed (9.5s)`; full LAN run returned `84 passed (1.4m)`.
- [x] Specialist PMO passes completed. Evidence: Peirce QA/Frontend found the missing pending-email page and selector risks; Parfit Backend/Security required transaction, safe next, email verification and strict pending payload; Confucius Verifier/Auditor passed security controls and originally blocked only runtime Playwright, which is now closed by LAN focused/full E2E.
- [blocked] Production auth remains blocked. Evidence: LAN still uses mock OAuth and console email; real Google/Facebook, OAuth encryption secret and SMTP provider delivery are not live in a staging/prod app.

## 2026-06-19 PMO Iteration - Static Staging Secret Contract

- [x] Static staging contract exists outside the live Argo path. Evidence: `staging/libreplay-staging-contract.yaml` defines only `Namespace/libreplay-staging`, `ExternalSecret/libreplay-secrets` and `ConfigMap/libreplay-config`; it does not define workloads, services, ingress or Argo app.
- [x] Staging secret path is separated from LAN. Evidence: every `remoteRef` points to `secret/libreplay/staging`, not `secret/libreplay`.
- [x] Real auth provider key names are mandatory in the static contract. Evidence: `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `FACEBOOK_CLIENT_ID`, `FACEBOOK_CLIENT_SECRET`, `OAUTH_TOKEN_ENC_KEY`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` are explicit `ExternalSecret.spec.data[*].secretKey` entries.
- [x] Staging config is real auth/email posture. Evidence: ConfigMap has `DEPLOYMENT_MODE=staging`, `NODE_ENV=production`, `USE_MOCK_OAUTH=false`, `AUTH_EMAIL_PROVIDER=smtp`, HTTPS `APP_BASE_URL`/`AUTH_URL`, non-local `MAIL_FROM`, and `ENABLE_LAN_DEMO_LOGIN=false`.
- [x] Static contract validates without applying. Evidence: `kubectl apply --dry-run=client -f staging/libreplay-staging-contract.yaml` returned Namespace, ExternalSecret and ConfigMap dry-run create messages.
- [x] Server-side staging namespace/app blocker was superseded by the contract-only Argo app. Evidence: current `Application/libreplay-staging` targets `path=staging`, namespace `libreplay-staging` exists, and `ExternalSecret/libreplay-secrets` exists but is `Ready=False SecretSyncedError` until Vault data is provided.
- [blocked] Real-provider staging remains blocked. Evidence: static contract contains key names only; actual Google, Facebook, OAuth encryption and SMTP values are still absent from the live runtime.

## 2026-06-19 PMO Iteration - Real Provider Staging Gate Scaffold

- [x] Source real-provider staging gate patch committed and pushed. Evidence: source commit `31b26c3 test(auth): add real provider staging gates`.
- [x] Staging app config is fail-closed for OAuth. Evidence: `DEPLOYMENT_MODE=staging` now rejects `USE_MOCK_OAUTH=true`, missing `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `FACEBOOK_CLIENT_ID`, `FACEBOOK_CLIENT_SECRET` and missing `OAUTH_TOKEN_ENC_KEY`.
- [x] Config/auth/web tests cover the stricter staging contract. Evidence: `packages/config` env tests include `rejects staging when mock OAuth remains enabled`; auth provider posture tests expect staging config failure instead of provider-disabled success; web CSRF staging fixture includes real-provider dummy keys.
- [x] Gated Playwright project exists without polluting default LAN suite. Evidence: default `npx playwright test --list` reports `79 tests in 22 files`; `PW_STAGING_REAL_AUTH=1 npx playwright test --list` reports `81 tests in 23 files`.
- [x] Real-provider staging smoke criteria are encoded. Evidence: `40-real-auth.staging.spec.ts` asserts Google OAuth start redirects to `accounts.google.com`, Facebook start redirects to `facebook.com`, and registration verification email drains `authEmail` with no failed jobs.
- [x] Source local and official validation passed. Evidence: `pnpm --filter @libreplay/config test` -> `15 passed`; `pnpm --filter @libreplay/auth test` -> `20 passed`; `pnpm --filter @libreplay/web typecheck` passed; `pnpm test` passed; `pnpm typecheck` passed; `pnpm --filter @libreplay/web build` passed; `pnpm audit --prod` -> `No known vulnerabilities found`; source CI run `27844167141` completed `success`.
- [x] Official release digests were published. Evidence: Release Image run `27844447288` completed `success`; web digest `sha256:b1c2ab433f9aa85d895b48e770809876d8efda6d54264ac61f75f0e073ee3529`; worker digest `sha256:a329d595cbaf608b183ea2deea7e187c6d7ef5c5f0b8d2809b389fd60d75354b`; tools digest `sha256:dc07aceea8e121e7d54c8a955e7e51e1cec2e1f7a8725f7a17b725ddbe5c6074`; seed digest `sha256:44e1d9a23788b626c29a903ed83bf22164840c04eb88c3031f2e5f79625c019b`.
- [x] Staging auth runbook and contract check exist. Evidence: [libreplay-staging-auth-runbook.md](./libreplay-staging-auth-runbook.md) and `scripts/check-libreplay-staging-contract.sh`; `sh -n scripts/check-libreplay-staging-contract.sh` and `git diff --check` passed.
- [x] Specialist PMO passes completed. Evidence: Wegener Security/Auth reported `BLOCKED` until real secrets/staging app exist; Kepler Architect/DevOps reported that no `libreplay-staging` app/namespace/ExternalSecret existed at that time and recommended Argo app + Kustomize overlay + per-env Vault path; Galileo PO/QA reported current E2E lacks real callback/inbox/linking smokes and prioritized gated staging smokes/runbook.
- [x] GitOps deploy/runtime evidence is complete. Evidence: GitOps commit `1b8ff18 feat: deploy libreplay staging auth gates`; GitOps CI run `27844721752` completed `success`; server-side dry-run passed; Argo is `Synced/Healthy` at `1b8ff18e7e29752982c6e7e20c6bf719005a58b0`; web and worker rollouts completed on `31b26c3` digests; `/api/health/deps=200` with queues all `0`; full LAN E2E returned `79 passed (1.3m)`; recent worker log grep for token/action URL/error patterns returned no matches.
- [blocked] Full real-provider staging remains blocked. Evidence: contract-only `libreplay-staging` app/namespace/ExternalSecret now exist, but `ExternalSecret/libreplay-secrets` is `Ready=False SecretSyncedError`; Vault data still lacks proven Google/Facebook/OAuth encryption/SMTP provider keys.

## 2026-06-19 PMO Iteration - Auth Email Queue And Staging Guardrails Gate

- [x] Source auth email queue patch committed and pushed. Evidence: source commit `9670441 feat(auth): queue transactional emails`.
- [x] Source console redaction patch committed and pushed. Evidence: source commit `53a8b48 fix(auth): redact console email action urls`.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/auth test`; `pnpm test`; `pnpm typecheck`; `pnpm --filter @libreplay/web build`; `pnpm audit --prod`; source CI run `27842739635` completed `success`.
- [x] Auth email queue is wired through routes and worker. Evidence: `register` and `forgot-password` enqueue `auth-email` jobs, `packages/jobs` has a separate `auth-email` BullMQ queue/handler, worker starts auth-email and media workers, and `/api/health/deps` exposes queue snapshots for both queues.
- [x] Staging/prod SMTP/OAuth guardrails fail closed. Evidence: env validation requires `NODE_ENV=production` for `DEPLOYMENT_MODE=staging|production`; staging/prod require `AUTH_EMAIL_PROVIDER=smtp`, `SMTP_HOST`, `SMTP_PORT`, non-local `MAIL_FROM`; `USE_MOCK_OAUTH=false` requires Google/Facebook secrets plus `OAUTH_TOKEN_ENC_KEY`.
- [x] Job retention avoids completed token-bearing payload retention. Evidence: `auth-email` jobs use `removeOnComplete: true`; failed jobs are bounded to age `3600s` and count `25`.
- [x] Console auth-email logs do not include action URLs or tokens. Evidence: final worker logs show `[auth-email:password_reset] queued console delivery to=demo-member@libreplay.local` and `auth-email completed 2 password_reset`; `rg 'token=|url=|reset-password|verify-email|password-reset|actionUrl'` against recent worker logs returned no matches.
- [x] Official release digests were published. Evidence: Release Image run `27843010658` completed `success` for source `53a8b4845725`; web digest `sha256:05ad264d771d1375949583ce4af2383133fe07ad4eb246706c343347d2fc6abe`; worker digest `sha256:f28dea3a5d6c5c367f4aa01ea3e7809e1d65376811b6e1b419708223330045c7`; tools digest `sha256:a2a4001b9e3ac01c903eb401846781132dbd563352e0b8058b9bb8ebcc229fc1`.
- [x] GitOps deploys web and worker by digest. Evidence: GitOps commit `6ca7ede fix: deploy redacted auth email logs`; GitOps CI run `27843274783` completed `success`; Argo is `Synced/Healthy` at `6ca7ede05399458c3ba42da9439e638ee0ba52aa`.
- [x] Runtime is on the expected revision. Evidence: `libreplay-web` is `1/1` on `sha-53a8b4845725@sha256:05ad264d771d1375949583ce4af2383133fe07ad4eb246706c343347d2fc6abe`; `libreplay-worker` is `1/1` on `worker-sha-53a8b4845725@sha256:f28dea3a5d6c5c367f4aa01ea3e7809e1d65376811b6e1b419708223330045c7`.
- [x] Runtime health and queues are clean. Evidence: `/api/health/deps=200` with postgres/redis/minio/meilisearch `ok:true`; `authEmail` and `mediaModeration` waiting/active/delayed/failed all `0`.
- [x] Focused auth E2E remains green. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 npx playwright test e2e/03-auth.anon.spec.ts --reporter=line` -> `7 passed (4.4s)`.
- [x] Full LAN E2E remains green with mobile smoke. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 npx playwright test --reporter=line` from `apps/web` -> `79 passed (1.3m)`, including mobile tests `77-79`.
- [x] Runtime secret posture is explicitly still LAN/demo. Evidence: live env has `DEPLOYMENT_MODE=lan-demo`, `USE_MOCK_OAUTH=true`, `AUTH_EMAIL_PROVIDER=console`, local `MAIL_FROM`; live `libreplay-secrets` key names do not include Google, Facebook, OAuth encryption or SMTP keys.
- [x] Independent verifier pass completed. Evidence: Lagrange initially reported FAIL because completed/failed auth-email jobs retained action URLs in Redis; after `removeOnComplete: true` and bounded failed retention, Lagrange re-audited PASS for this LAN sub-hito with residual risk documented.
- [blocked] Full PROD-3C production readiness remains blocked. Evidence: no real Google/Facebook/SMTP/OAuth encryption secrets are present in runtime, no `DEPLOYMENT_MODE=staging` app exists, `USE_MOCK_OAUTH=true` remains active in LAN, and no real-provider/SMTP smoke has been run.

## 2026-06-19 PMO Iteration - OAuth/CSRF Hardening Gate

- [x] Source OAuth/CSRF hardening patch committed and pushed. Evidence: source commit `c37cdca fix(auth): harden oauth and origin checks`.
- [x] Source validation passed. Evidence: `pnpm typecheck`; `pnpm test`; `pnpm --filter @libreplay/web build`; `pnpm audit --prod`; source CI run `27839801643` completed `success` with typecheck, unit tests, web build and Docker smoke.
- [x] Auth package hardening tests pass. Evidence: `pnpm --filter @libreplay/auth test` -> `20 passed`, covering OAuth flow cookie sealing/tamper/expiry, Google id-token claim normalization, Facebook email trust, provider posture, token encryption fail-closed and account-linking conflicts/race.
- [x] Web/security origin tests pass. Evidence: `pnpm --filter @libreplay/security test` -> `12 passed`; `pnpm --filter @libreplay/web test` -> `11 passed`, including production fail-closed Origin handling, staging same-origin/cross-site behavior and LAN no-Origin compatibility.
- [x] OAuth protocol is hardened enough before real-provider staging. Evidence: Google uses PKCE S256, nonce and `jose` `id_token` validation for issuer/audience/nonce before trusting `sub`; Facebook sends documented plain PKCE verifier/challenge and never treats Graph `email` as verified; OAuth flow metadata is AES-GCM sealed with TTL and provider+state cookie scoping.
- [x] Account linking is session-bound and conflict-safe. Evidence: `start?intent=link` requires a session and seals `linkSessionId/linkUserId`; callback rejects session mismatch; linking checks provider-account and provider-email conflicts; same-user update no longer rewrites `userId`; P2002 provider-account races return `email_conflict`.
- [x] Sensitive auth/account POST routes enforce Origin/Fetch Metadata. Evidence: `/api/auth/login`, `register`, `logout`, `forgot-password`, `reset-password`, `verify-email`, `demo-login`, `demo-reset` and `/api/me/security/unlink-social` call `enforceAuthOrigin` before mutating state or consuming rate limits.
- [x] Official release digests were published. Evidence: Release Image run `27840087320` completed `success` for source `c37cdcad6f06`; web digest `sha256:fd2ed9c2553eb5d57d36c71774a91e638713742f15e06b2816009d2c857855f9`; tools digest `sha256:74b4b91f3ec0f2c55886ddec27afa56fe437decf4dcef125cd77a29a1763be3f`; worker digest `sha256:46b77b2244d949d8b308780aa7451192d0f5ac47a0ed4ed4d0f26e9341381371`; seed digest `sha256:60deb5a27260d5e509caa2802752f4540bba669a13e6b8cc196be87853ad21b0`.
- [x] GitOps deploys web, worker and migration by digest. Evidence: GitOps commit `a3a6567 feat: deploy libreplay oauth csrf hardening`; GitOps CI run `27840418461` completed `success`; `kubectl apply --dry-run=server -f k8s/manifest.yaml` passed; obsolete completed job `libreplay-db-migrate-1bbe568` was deleted after new job succeeded because Argo has `prune=false`.
- [x] Runtime is on the expected revision. Evidence: Argo `Synced/Healthy` at `a3a6567743ba1fff364c02f87650f2d9c9126964`; `libreplay-web` uses `sha-c37cdcad6f06@sha256:fd2ed9c2553eb5d57d36c71774a91e638713742f15e06b2816009d2c857855f9`; `libreplay-worker` uses `worker-sha-c37cdcad6f06@sha256:46b77b2244d949d8b308780aa7451192d0f5ac47a0ed4ed4d0f26e9341381371`; `Job/libreplay-db-migrate-c37cdca` completed `1/1`.
- [x] Runtime auth/OAuth E2E remains green. Evidence: focused auth Playwright `e2e/03-auth.anon.spec.ts` -> `7 passed`, including cross-site Origin rejection for login/forgot/reset/verify/logout/unlink-social; OAuth start smoke `e2e/14-coverage-extras.anon.spec.ts -g "OAuth start"` -> `1 passed`.
- [x] Full LAN E2E remains green with mobile smoke. Evidence: full LAN Playwright `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-oauth-csrf-full.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `79 passed (1.3m)`, including mobile tests `77-79`.
- [x] Runtime health remains clean. Evidence: `/api/health/deps=200` with postgres/redis/minio/meilisearch `ok:true`; pods/jobs are running/completed with zero restarts; Redis `bull:media-moderation` wait/active/delayed/failed all `0`; recent web/worker logs contain no `error`, `exception` or `failed` matches.
- [x] Independent verifier pass completed. Evidence: Hilbert first reported FAIL for Facebook PKCE documentation, account-linking race and missing tests; after fixes, Hilbert re-audited and reported PASS for committing/deploying PROD-3B.
- [blocked] Production readiness remains blocked. Evidence: real Google/Facebook credentials are not present/enabled in runtime, `USE_MOCK_OAUTH=true` remains LAN-only, real SMTP queue/retry/observability is absent, payments are mock-only, video HLS/renditions are absent, and production HA/DR/compliance/observability gates remain open.

## 2026-06-19 PMO Iteration - Auth Email Recovery Gate

- [x] Source auth email recovery patch committed and pushed. Evidence: source commit `1bbe568 feat(auth): add email recovery groundwork`.
- [x] Source validation passed. Evidence: `pnpm install --frozen-lockfile`; `DATABASE_URL=postgresql://libreplay:secret@localhost:55432/libreplay pnpm --filter @libreplay/db exec prisma validate`; `pnpm typecheck`; `pnpm test`; `pnpm --filter @libreplay/web build`; `pnpm audit --prod`; Playwright list reports `78 tests`.
- [x] Auth package now has direct tests. Evidence: `pnpm --filter @libreplay/auth test` -> `8 passed`, covering token purpose hashing/expiry, OAuth mock posture, OAuth token encryption fail-closed and console email blocked in staging.
- [x] Email verification/reset token schema is deployed. Evidence: migration `20260619181500_auth_email_tokens`; DB has `EmailVerificationToken` and `PasswordResetToken`; `AuditAction` enum has `EMAIL_VERIFICATION_REQUESTED`, `EMAIL_VERIFIED`, `PASSWORD_RESET_REQUESTED`, `PASSWORD_RESET_COMPLETED`.
- [x] Forgot/reset/verify/register routes are wired without email enumeration. Evidence: `forgot-password` creates a reset token only when a user exists but always returns OK; `reset-password` consumes one-time reset tokens; `verify-email` consumes one-time verification tokens; `register` creates verification tokens outside auto-verified dev/LAN/test posture.
- [x] OAuth base guardrails improved. Evidence: mock OAuth providers are only selected in test/development/LAN demo; staging/production with missing provider credentials returns provider disabled instead of silently falling back to mocks; OAuth token encryption throws in staging/production when `OAUTH_TOKEN_ENC_KEY` is absent.
- [x] Pending social cookie no longer carries provider tokens. Evidence: OAuth callback stores pending-social profile with `accessToken=null`, `refreshToken=null`, `raw={}`.
- [x] Official release digests were published. Evidence: Release Image run `27837784400` completed `success` for source `1bbe56839b7e9f9b60210f1a358648bb2e532d98`; web digest `sha256:cea09cfc03b8d0d9998ae424d52624c9b6047c4abd525f0c84ef93f61743efc3`; tools digest `sha256:a29b311546377fa76e0d5cd6e2539550529db4489b5b71efb2abc9da8f35d532`; worker digest `sha256:625b951057d52913ac7315317b809a52e6e9dba69d5b31a660d7293be5f2cd9d`; seed digest `sha256:64ee02daf563d9bf44b0b10d986b402aef3806719a2672dec3048c0535137cef`.
- [x] GitOps deploys web, worker and migration by digest. Evidence: GitOps commit `da4868b feat: deploy libreplay auth email groundwork`; GitOps CI run `27838078792` completed `success`; `kubectl apply --dry-run=server -f k8s/manifest.yaml` passed; obsolete completed job `libreplay-db-migrate-282d735` was deleted after new job succeeded because Argo has `prune=false`.
- [x] Runtime is on the expected revision. Evidence: Argo `Synced/Healthy` at `da4868bb8bcdaedfcc68a0c6257b7c0d81654349`; `libreplay-web` uses `sha-1bbe56839b7e@sha256:cea09cfc03b8d0d9998ae424d52624c9b6047c4abd525f0c84ef93f61743efc3`; `libreplay-worker` uses `worker-sha-1bbe56839b7e@sha256:625b951057d52913ac7315317b809a52e6e9dba69d5b31a660d7293be5f2cd9d`; `Job/libreplay-db-migrate-1bbe568` completed `1/1`.
- [x] LAN auth E2E remains green. Evidence: focused auth Playwright `e2e/03-auth.anon.spec.ts` -> `6 passed`, including forgot-password confirmation, invalid reset token rejection and invalid verify-email token rejection.
- [x] Full LAN E2E remains green with mobile smoke. Evidence: full LAN Playwright `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-auth-full.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `78 passed (1.3m)`, including mobile tests `76-78`.
- [x] Runtime health remains clean. Evidence: `/api/health=200`; `/api/health/deps=200` with postgres/redis/minio/meilisearch `ok:true`; pods are running/completed with zero restarts; worker logs show `media-moderation completed 15/16`; Redis `bull:media-moderation` wait/active/delayed/failed all `0`.
- [x] Independent auth/security verifier passes completed. Evidence: Pascal and Ramanujan identified the PROD-3 blockers; Halley reported PASS for committing source PROD-3A after verifying schema, route wiring, OAuth guardrails, token storage and tests.
- [blocked] Production readiness remains blocked. Evidence: OAuth/CSRF code hardening is now closed by the later OAuth/CSRF gate, but real SMTP/OAuth secrets are absent from GitOps, email delivery has no queue/retry/observability, and the app still runs LAN mocks.

## 2026-06-19 PMO Iteration - Video Groundwork Gate

- [x] Source video groundwork patch committed and pushed. Evidence: source commits `a55c0f6 feat(media): add video probe thumbnails` and `e9a1135 fix(media): bound video processing failures`.
- [x] Source validation passed. Evidence: `pnpm --filter @libreplay/media test` -> `7 passed`; `pnpm --filter @libreplay/jobs test` -> `5 passed`; focused media/jobs typechecks passed; source CI run `27835662179` completed `success`.
- [x] Official release digests were published. Evidence: Release Image run `27836030980` completed `success` for head `e9a11353e9bc51637405a8160b02f000effbf408`; web digest `sha256:45a5e0a804618f780e38e29800aea921bee46f8845154778a46b508db1f4159f`; worker digest `sha256:10b3bfca297d954590dcce9dc8f6d37195335c28815340cd28c9bf6733209da5`; tools digest `sha256:dd14ec3ac07e18a424e324eae8654a363c0fc737d05e543a04e7b72d5529383b`; seed digest `sha256:9744acc106a9ed34147a158533bedf265b902e76648b748882edf5315f995d90`.
- [x] GitOps deploys web and worker by digest. Evidence: GitOps commit `e576d68 feat: deploy libreplay video groundwork`; GitOps CI run `27836441318` completed `success`; `kubectl apply --dry-run=server -f k8s/manifest.yaml` passed.
- [x] Runtime is on the expected revision. Evidence: Argo `Synced/Healthy` at `e576d68442ed5438fc1347d24045ae7a33f9ce50`; `libreplay-web` uses `sha-e9a11353e9bc@sha256:45a5e0a804618f780e38e29800aea921bee46f8845154778a46b508db1f4159f`; `libreplay-worker` uses `worker-sha-e9a11353e9bc@sha256:10b3bfca297d954590dcce9dc8f6d37195335c28815340cd28c9bf6733209da5`; both rollouts completed.
- [x] Worker runtime can process video. Evidence: `kubectl -n libreplay exec deploy/libreplay-worker -- ffmpeg -version` -> `ffmpeg version 5.1.9-0+deb12u1`; `ffprobe -version` -> `ffprobe version 5.1.9-0+deb12u1`.
- [x] Video uploads are probed and thumbnail variant is generated. Evidence: focused LAN Playwright `e2e/33-payments-media.auth.spec.ts` -> `5 passed`, including `video upload is probed and produces a thumbnail variant`.
- [x] Video metadata and thumbnail row exist in DB. Evidence: `MediaAsset` `cmql4l8dw000d31zblcy4a08a` is `VIDEO`, `video/mp4`, `APPROVED`, `durationSeconds=1`, `width=16`, `height=16`; `MediaVariant` has `THUMB_LARGE`, `image/jpeg`, `variants/cmql4l8dw000d31zblcy4a08a/video_thumbnail.jpg`, `sizeBytes=222`, `width=16`, `height=16`.
- [x] Video original and thumbnail exist in S3-compatible storage. Evidence: temporary `minio/mc` pod `mc stat --json` found original `video/cmqk3dkr800006gyso70h639f/1781885449651-lan-demo-video.mp4` with `Content-Type=video/mp4`, size `2210`; thumbnail `variants/cmql4l8dw000d31zblcy4a08a/video_thumbnail.jpg` with `Content-Type=image/jpeg`, size `222`.
- [x] Queues and worker are healthy after video processing. Evidence: worker logs show `media-moderation completed 11` and `12`; Redis `bull:media-moderation` wait/active/delayed/failed all `0`.
- [x] LAN E2E remains green with mobile smoke. Evidence: full LAN Playwright `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-video-full.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `76 passed (1.3m)`, including mobile tests `74-76`.
- [x] Independent DevOps/release verifier pass completed. Evidence: Volta reported PASS for GitOps CI `27836441318`, Argo `Synced/Healthy`, expected e9a1135 digests, ffmpeg/ffprobe runtime and no failed pods/jobs.
- [blocked] Production readiness remains blocked. Evidence: this gate adds probe/thumbnail/max-duration groundwork, but production video renditions/HLS, real CSAM/media moderation providers, HA/backups/observability, real OAuth/email and production payments remain open.

## 2026-06-19 PMO Iteration - Image Variants Gate

- [x] Source image variants patch committed and pushed. Evidence: source commit `282d735 feat(media): generate image variants`.
- [x] Source CI and release automation passed. Evidence: source CI run `27833272292` completed `success`; Release Image run `27833556729` completed `success` for head `282d7355f43eedfae875f7307eb8f30710966a7a`.
- [x] Official image digests were published. Evidence: web `sha256:aac3233831286fc38747f0fd5bd81107174532c7bcfabc4bd4aa903d0d714d66`; tools `sha256:602b39a0b85ff1f3007df22caf410aa294247db59f63ff4d885de95a7f751979`; worker `sha256:fa93aabbca6c3db25851d5cae6d17efd401f28554f39867909adad9b8fc399cb`.
- [x] GitOps deploys web, worker and migration by digest. Evidence: GitOps commit `e92f703 feat: deploy libreplay image variants`; GitOps CI run `27833929688` completed `success`; `kubectl apply --dry-run=server -f k8s/manifest.yaml` passed.
- [x] Runtime is on the expected revision. Evidence: Argo `Synced/Healthy` at `e92f703c606a2d56624132afa9b923c633535118`; `libreplay-web` and `libreplay-worker` rollouts completed with `sha-282d7355f43e` images.
- [x] Database migration applied. Evidence: `Job/libreplay-db-migrate-282d735` completed `1/1`; DB has `MediaVariant.mimeType`, `MediaVariant.sizeBytes`, unique index `MediaVariant_mediaAssetId_kind_mimeType_key` and unique index `MediaVariant_objectKey_key`.
- [x] Worker generates image variants. Evidence: focused media E2E generated asset `cmql2t9ll002htbd0mkoperbu`; DB has five variants: `THUMB_SMALL image/webp`, `THUMB_MEDIUM image/webp`, `THUMB_MEDIUM image/avif`, `THUMB_LARGE image/webp`, `BLURRED_PREVIEW image/webp`, each with `sizeBytes`, width and height.
- [x] Variant objects exist in S3-compatible storage. Evidence: temporary `minio/mc` pod `mc stat` found original `image/png` object plus five variant objects under `variants/cmql2t9ll002htbd0mkoperbu/` with `Content-Type` `image/webp` or `image/avif` and non-zero sizes.
- [x] Variant metadata endpoint is protected. Evidence: anonymous GET `/api/media/cmql2t9ll002htbd0mkoperbu/variants` returned `403 AUTH_REQUIRED`; creator non-owner session returned `403 UNATTACHED`.
- [x] Queues and dependencies are healthy after processing. Evidence: worker logs show jobs `9` and `10` completed; Redis `bull:media-moderation` wait/active/delayed/failed all `0`; `/api/health/deps` reports postgres, redis, minio and meilisearch `ok:true`.
- [x] LAN E2E remains green with mobile smoke. Evidence: focused media Playwright `3 passed`; full LAN Playwright `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-variants-full.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `74 passed (1.8m)`, including mobile tests `72-74`.
- [x] Independent DevOps and QA/media verifier passes completed. Evidence: Franklin reported GitOps manifest PASS for the six expected changes; Beauvoir reported validation-plan PASS only after DB/Redis/worker/S3/perms gates, which were executed and recorded above.
- [blocked] Production readiness remains blocked. Evidence: video pipeline, real CSAM/media moderation providers, HA/backups/observability, real OAuth/email and production payments remain open.

## 2026-06-19 PMO Iteration - Media Worker Foundation Gate

- [x] Source media foundation patch committed and pushed. Evidence: source commit `1d86d1d feat(media): stream uploads and add worker image`.
- [x] `/api/media/upload/[id]` no longer buffers the whole file in web memory. Evidence: [upload route](/home/dibanez/k8s/libreplay/apps/web/src/app/api/media/upload/[id]/route.ts:1) uses `Readable.fromWeb(req.body)` with `ContentLength`; `req.arrayBuffer()` is removed from the media upload route.
- [x] `/api/media/complete` validates S3 object existence/metadata before enqueue. Evidence: [complete route](/home/dibanez/k8s/libreplay/apps/web/src/app/api/media/complete/route.ts:1) calls `headObject`; missing uploads return `409 UPLOAD_NOT_FOUND`, size mismatch returns `400 SIZE_MISMATCH`, storage errors return `503 STORAGE_UNAVAILABLE`.
- [x] `/api/media/complete` no longer falls back to inline processing. Evidence: route imports only `enqueueMediaModeration`; enqueue failure returns `503 MEDIA_QUEUE_UNAVAILABLE` and reverts the asset to `PENDING_UPLOAD`.
- [x] Dedicated worker image is built and published by release automation. Evidence: source CI run `27831861195` completed `success`; Release Image run `27832114949` completed `success` and published worker digest `sha256:4a156bdd8b6851a0d5028a62c886b452f61bc67655a851991086bdae8091fbe5`.
- [x] GitOps deploys web and worker by digest. Evidence: GitOps commit `e17dc06 feat: deploy libreplay media worker`, GitOps CI run `27832428507` completed `success`; server-side dry-run reported `deployment.apps/libreplay-worker created`.
- [x] Runtime worker is healthy and drains media jobs. Evidence: Argo revision `e17dc062121cd2ca6889625d8bc3210f2675fe0d` is `Synced/Healthy`; `libreplay-web` and `libreplay-worker` are `1/1`; worker logs show `[jobs] worker started` and jobs `1-8` completed; Redis `bull:media-moderation:wait=0` and `failed=0`.
- [x] LAN E2E remains green on the media worker release. Evidence: focused media Playwright `3 passed`; full LAN Playwright `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-media-worker-full.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `74 passed (1.2m)`.
- [x] Independent backend, DevOps and release verifier passes completed. Evidence: Pauli reported read-only backend media checklist PASS; Lovelace reported worker deployment checklist PASS with residual production blockers; Nietzsche reported release/media verification PASS.
- [blocked] Production readiness remains blocked. Evidence: video transcoding, real CSAM/media moderation providers, HA/backups/observability, real OAuth/email and production payments remain open.

## 2026-06-19 PMO Iteration - Release Automation Gate

- [x] GitHub release secrets exist by name without exposing values. Evidence: `gh secret list --repo pocharlies-org/libreplay` showed `HARBOR_USER` and `HARBOR_PASSWORD`, updated `2026-06-19T14:12:39Z/14:12:40Z`.
- [x] `Release Image` workflow is active and dispatchable. Evidence: [release.yml](/home/dibanez/k8s/libreplay/.github/workflows/release.yml:1) has `workflow_dispatch`; `gh workflow list` showed it `active`.
- [x] Release workflow publishes web/tools/seed without manual Harbor push. Evidence: `pocharlies-org/libreplay` run `27830810054` completed `success`; `docker/login-action@v3` and `Build and push image` steps passed.
- [x] Official release digests exist. Evidence: `harbor.e-dani.com/homelab/libreplay-web:sha-b3ebad4ac482@sha256:d7eeb53810159c9bc7fc3ed5355eac4d094ba1e7b213932b72ef3601d85df26c`, tools digest `sha256:0b76bf0b6af981dd6a359f99c3788977d8d13ec45eacecac351beaaf1caabe4b`, seed digest `sha256:1593f9054629e2d5de3e151be8ca19b3009cc37645156fc7975726f83f75161c`.
- [x] GitOps deploys the official release web digest. Evidence: GitOps commit `1329105 fix: deploy libreplay release workflow image`, GitOps CI run `27831093880` completed `success`; Argo revision `1329105487400b531ebef6c3eb0541288bdb9ba5` is `Synced/Healthy`, web deployment is `1/1`, observedGeneration `20/20`.
- [x] Full LAN Playwright remains green on the official release image. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-release-workflow-b3ebad4.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `73 passed (1.3m)`.
- [x] Independent release verifier pass completed. Evidence: subagent `019ee039-d912-7ee3-8991-4fa651cb98c8` reported PASS for secrets-by-name, workflow run `27830810054`, Harbor login, published digests and read-only preservation.
- [blocked] Production readiness remains blocked. Evidence: no real OAuth secrets/runtime, no real email verification/reset, payments are mock-only, no media compression/transcoding, no HA/backup/observability baseline.

## 2026-06-19 PMO Iteration - QA/Security Gate

- [x] Source validation patch committed and pushed. Evidence: source commits `97fac1a fix: harden lan demo validation` and `b3ebad4 fix: reset demo users in lan e2e setup`.
- [x] GitOps validation image deployed. Evidence: GitOps commits `779f55e fix: deploy libreplay validation image` and `eb96c48 fix: deploy libreplay demo reset image`.
- [x] Runtime image is pinned by digest. Evidence: `harbor.e-dani.com/homelab/libreplay-web:sha-b3ebad4ac482@sha256:a22212a195bb5e88d1e3f62c9a8b2a60ffb8c2dc2f0068fe36b75d0008996ba1`.
- [x] Argo is current and healthy. Evidence: Argo `Synced/Healthy`, revision `eb96c4880dd495ea265962623239f36723be8122`, operation `Succeeded`.
- [x] Health and dependency smoke pass. Evidence: `/api/health=200`, `/api/health/deps=200`.
- [x] LAN demo reset is protected and functional. Evidence: `/api/auth/demo-reset` without header returned `403`; with `x-libreplay-demo-reset: playwright` returned `200` and `updatedUsers=9`.
- [x] Full LAN Playwright is green and repeatable from persistent data. Evidence: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWJSON=/tmp/libreplay-playwright-b3ebad4.json pnpm --filter @libreplay/web exec playwright test --reporter=list` -> `73 passed (1.3m)`.
- [x] Production dependency audit is clean. Evidence: `pnpm audit --prod` -> `No known vulnerabilities found`.
- [x] Source CI passed. Evidence: `pocharlies-org/libreplay` CI run `27830389764` completed `success`.
- [x] GitOps CI passed. Evidence: `pocharlies-org/k8s-libreplay-pocharlies` CI run `27830499649` completed `success`.
- [blocked] Production readiness remains blocked. Evidence: no real OAuth secrets/runtime, no real email verification/reset, payments are mock-only, no media compression/transcoding, no HA/backup/observability baseline.

## Directives

- [x] No exposicion publica; solo LAN/SSO existente. Evidence: `k8s/manifest.yaml` keeps only `IngressRoute/libreplay-lan`.
- [x] No importar ni borrar datos antiguos. Evidence: rollout uses database `libreplay_lan`; no PVC deletion or data import is encoded.
- [x] Sin passwords para usuarios/QA. Evidence: source commit `986ceec2b562...` uses `/api/auth/demo-login`; Playwright no longer references `SEED_AI_USER_PASSWORD` or `E2E_TEST_PASSWORD`.
- [x] Secrets internos siguen en secrets. Evidence: manifest reads `DATABASE_URL`, `REDIS_URL`, `AUTH_SECRET`, MinIO, Meili and `SEED_USER_PASSWORD` from `libreplay-secrets`.
- [x] Mocks visibles como LAN/no-prod. Evidence: ConfigMap enables mock flags; UI has demo/no-prod panel and creator/payment mock copy.
- [x] LAN demo cannot be mistaken for production mode. Evidence: GitOps ConfigMap declares `DEPLOYMENT_MODE=lan-demo`; source env parser rejects `ENABLE_LAN_DEMO_LOGIN` outside `lan-demo` and rejects all critical mocks in `DEPLOYMENT_MODE=production`; web pod template carries config rollout annotation `deployment-mode-lan-demo-20260619-1302` so pods reload ConfigMap env.
- [x] No cerrar con pods caidos, 404s, buttons mudos, skips criticos or missing secrets in LAN validation. Evidence: current full LAN Playwright is `79 passed`, Argo is `Synced/Healthy`, web and worker pods are `1/1`, and reset/rate-limit issues are fixed for `DEPLOYMENT_MODE=lan-demo`.

## Acceptance Criteria

- [x] Argo `libreplay` `Synced/Healthy`. Evidence: revision `05f3c349a4ed07d4e67e9c31fc2cf01ff7732f22`, sync `Synced`, health `Healthy`, phase `Succeeded`.
- [x] Pods ready: Postgres, Redis, MinIO, Meili, web. Evidence: `kubectl -n libreplay get pods,jobs,deploy,endpointslice -o wide` shows Postgres/Redis/MinIO/Meili/web `1/1 Running`, jobs `Complete`, web endpoint `10.42.0.32:3000`.
- [x] `/api/health` and `/api/health/deps` return 200. Evidence: `curl -sk .../api/health` and `.../api/health/deps` returned `HTTP 200`; deps checks postgres/redis/minio/meilisearch all `ok:true`.
- [x] Demo login works for member, moderator, admin, creator, club owner and newbie without typing passwords. Evidence: API smoke returned `200` for member `/feed`, moderator `/admin`, admin `/admin`, creator `/creator/dashboard`, clubowner `/events/new`, newbie `/onboarding`; newbie repeated twice to prove reset/reusability.
- [x] Feed, discover, friends, messages, profiles, groups, clubs, dates, events, forum, blog, map, creator, admin, verification mock and payments mock work. Evidence: full Playwright LAN suite passed all feature specs, including creator payments mock, verification mock, media upload proxy and role projects.
- [x] Playwright LAN full suite passes with trace/screenshot/video and zero critical skips. Evidence: `/tmp/libreplay-playwright-results.json` stats `expected=73`, `unexpected=0`, `flaky=0`, `skipped=0`; artifacts: `73` trace zips, `72` screenshots, `72` videos, HTML report present. One API-only test has no page screenshot/video.
- [x] Checklist final updated with commands, outputs and resolved blockers. Evidence: this file updated after final Argo, smoke, policy and Playwright verification.

## Source / Build

- [x] LAN demo source committed. Evidence: `/home/dibanez/k8s/libreplay` commits through `2365959 fix: make LAN demo e2e repeatable` pushed to `origin/main`.
- [x] Source canonical repo is in the ARC-backed org. Evidence: `origin` for `/home/dibanez/k8s/libreplay` is `git@github.com:pocharlies-org/libreplay.git`; previous personal repo remains as remote `personal` for traceability.
- [x] Source CI runs on ARC `arc-k8s` and is green. Evidence: `pocharlies-org/libreplay` run `27820394287` completed successfully on 2026-06-19; job `verify` passed `checkout`, `pnpm/action-setup`, `setup-node`, `pnpm install --frozen-lockfile`, Prisma generate, typecheck, unit tests, web build and Docker build smoke in `5m45s`.
- [x] Production safety env guardrails have unit/CI coverage. Evidence: source commit `61c6dff feat(config): add deployment mode guardrails`; tests cover `DEPLOYMENT_MODE=lan-demo`, production mock rejection, demo-login rejection outside LAN demo and strict string boolean parsing; local `pnpm test`, `pnpm typecheck`, `pnpm --filter @libreplay/web build` passed; `pocharlies-org/libreplay` CI run `27821491050` passed `typecheck`, unit tests, web build and Docker build smoke in `4m59s`.
- [x] Guardrail image published for runtime. Evidence: local Docker build/push from source commit `61c6dff`; image `harbor.e-dani.com/homelab/libreplay-web:sha-61c6dffb1b0c@sha256:46a00da87e50fb0dae9f77d4b5e458902e1e422eab82f7a732b91bd4380bfb43`.
- [x] `pnpm install --frozen-lockfile`. Evidence: exited 0; lockfile up to date and already up to date.
- [x] `pnpm typecheck`. Evidence: exited 0 on 2026-06-19.
- [x] `pnpm test`. Evidence: exited 0; config 4 tests, security 7 tests, auth no-tests pass, web 8 tests.
- [x] `pnpm lint`. Evidence: exited 0; one pre-existing Next font warning only.
- [x] `pnpm --filter @libreplay/web build`. Evidence: exited 0 and generated routes including `/api/auth/demo-login`, `/api/health/deps`, `/api/media/upload/[id]`.
- [x] Docker runner/tools/seed smoke builds. Evidence: local `docker build --target runner|tools|seed` exited 0.

## Images / Harbor

- [x] Web image pushed and inspected. Evidence: `harbor.e-dani.com/homelab/libreplay-web:sha-61c6dffb1b0c@sha256:46a00da87e50fb0dae9f77d4b5e458902e1e422eab82f7a732b91bd4380bfb43`.
- [x] Migrate tools image pushed and inspected. Evidence: `harbor.e-dani.com/homelab/libreplay-web:tools-sha-59e051279464@sha256:d5183716a46f849f8f1df8676767f2c00a92cd5e29e1d2b1caf8bb51cbd029db`.
- [x] Seed image pushed and inspected. Evidence: `harbor.e-dani.com/homelab/libreplay-web:seed-sha-15da141534f1@sha256:d1c44a5a95ae5330f8f1ce42b25412f88d702b65bee3dc58ce872e67142bfa77`.
- [x] Image labels match source. Evidence: web image label `org.opencontainers.image.revision=61c6dffb1b0cd177cf54e5eded0ebee4140d7d09`; tools returned `59e0512794648a4ce46c5e90007ebbfabedd4099`; seed returned `15da141534f1d75c4d638940113875b02e2aba00`.

## GitOps

- [x] Work is on Argo target branch. Evidence: repo branch `deploy/prod`, Argo target revision previously verified as `deploy/prod`.
- [x] Web image pinned by digest; no `latest`. Evidence: `k8s/manifest.yaml`.
- [x] Datastores scale to one replica. Evidence: StatefulSet/Deployments for Postgres, Redis, Meili and MinIO use `replicas: 1`.
- [x] DB fresh target is `libreplay_lan`. Evidence: Postgres `POSTGRES_DB=libreplay_lan`; `libreplay-db-init` creates DB if existing PVC lacks it.
- [x] MinIO bucket init is explicit. Evidence: `Job/libreplay-minio-init`.
- [x] Migrate and seed are real Jobs. Evidence: `Job/libreplay-db-migrate-59e0512` and `Job/libreplay-db-seed-15da141`.
- [x] Job memory limits present. Evidence: all Jobs set memory requests/limits.
- [x] Server-side manifest dry-run passes. Evidence: `kubectl apply --dry-run=server -f k8s/manifest.yaml` exited 0; only last-applied annotation warnings on pre-existing resources.
- [x] Live Argo sync complete. Evidence: GitOps commit `05f3c34 fix: update libreplay LAN web image` pushed; Argo revision `05f3c349a4ed07d4e67e9c31fc2cf01ff7732f22`, sync `Synced`, health `Healthy`.

## Runtime Secrets

- [x] `harbor-pull` exists in namespace `libreplay`. Evidence: namespace pulls private Harbor images successfully; web pod pulled `sha-23659593fb24` and jobs pulled tools/seed images.
- [x] Vault/ExternalSecret exposes required app keys. Evidence: key names only from `kubectl -n libreplay get secret libreplay-secrets`: `AUTH_SECRET`, `DATABASE_URL`, `DB_PASSWORD`, `DB_USER`, `MEILISEARCH_API_KEY`, `MEILI_MASTER_KEY`, `MINIO_ACCESS_KEY`, `MINIO_BUCKET`, `MINIO_ROOT_PASSWORD`, `MINIO_ROOT_USER`, `MINIO_SECRET_KEY`, `REDIS_URL`, `SEED_USER_PASSWORD`.
- [x] Generated internal secrets are not printed or committed. Evidence: final verification lists only secret key names; `git status --short` clean in source and GitOps repos.

Required keys: `DATABASE_URL`, `REDIS_URL`, `AUTH_SECRET`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `MINIO_BUCKET`, `MEILISEARCH_API_KEY`, `DB_USER`, `DB_PASSWORD`, `SEED_USER_PASSWORD`.

## Specialist Checks

- [x] Research/PMO pass. Evidence: memory source `rollout-2026-06-18T18-26-33...` used as clue and revalidated with repo/cluster commands.
- [x] DevOps read-only pass. Evidence: subagent reported blockers: missing `harbor-pull`, incomplete secret contract, job policy failures, no endpoints.
- [x] QA/Security read-only pass. Evidence: subagent reported blockers: local source not deployed, demo session risk, missing Playwright demo coverage.
- [x] Backend/frontend implementation pass. Evidence: demo login, health deps, upload proxy, seed and Playwright role projects implemented in commit `986ceec`.
- [x] Runtime verification pass. Evidence: Argo/pods/health/demo roles/policy reports/Playwright LAN all passed on 2026-06-19.

## Final Verification Evidence

- [x] Playwright LAN: `BASE_URL=https://libreplay.lan.e-dani.com PWRETRIES=0 PWSCREENSHOT=on PWVIDEO=on PWJSON=/tmp/libreplay-playwright-results.json npx playwright test --trace on` -> `73 passed` in `1.6m`, `skipped=0`, `unexpected=0`, `flaky=0`.
- [x] Artifacts archived locally. Evidence: `apps/web/playwright-report` 88M, `apps/web/test-results` 87M, `/tmp/libreplay-playwright-results.json` 172K.
- [x] Policy reports clear for P0/P1. Evidence: no critical/high failures in namespaced `policyreport` or `clusterpolicyreport`.
- [x] Runtime logs checked. Evidence: web logs show Next ready; only non-blocking AWS SDK future Node >=22 warning.
- [x] Historical warning reconciled. Evidence: events still contain old `libreplay-db-migrate-5dffe73` backoff warning, but current `libreplay-db-migrate-59e0512` is `Complete` and Argo is `Healthy`.
